# TODO

> 单一真理源。**不另起 `docs/plans/`**。完成项从本文件删除，历史记录看 git log。
>
> 写作纪律：`docs/agent-memory/feedback_plan_writing_quality.md`。每条任务必须有明确验收与文档路径。

## 当前主线

1. **M1 iHawk 帧链路**：先把 Color/Depth 字节流、内参、预览跑实。
2. **M3 多视角 RGBD 重建**：阶段 1 先做端侧采集 + 云端融合闭环。
3. **M4 VIN 数码拓印**：复用同一 RGBD 采集链路，接 cv-engine `vin_pipeline`。
4. **M5 实时消息与第一视角协作**：消息控制面自研，视频 / 直播媒体面接自托管 LiveKit。
5. **M7 端侧两大功能拉通**：把"车辆外廓扫描"和"VIN 数码拓印"从 mock 演示屏接到已就绪的真实底座（CameraSource 真帧 + 服务端融合 / OCR），端侧中段补齐。

## M5 实时消息与第一视角协作

> 控制面：gomob 自研 WebSocket + REST + PostgreSQL，负责消息顺序、会话、未读、邀请、审计。
> 媒体面：自托管 LiveKit + coturn + Egress，负责视频通话、第一视角直播、录制。
> docs: `docs/architecture/09-realtime-message-live.md` / `docs/architecture/09-realtime-message-live-implementation.md`

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M5.4 | LiveKit 媒体控制面：接自托管 LiveKit 配置，服务端实现 `POST /v1/media/rooms`、`POST /v1/media/rooms/{id}/token`、`POST /v1/media/rooms/{id}/end`、`POST /v1/livekit/webhook`；`core:media` 接 LiveKit Android SDK，封装 `MediaRoomClient`。 | `tests/harness/livekit_room_lifecycle` 通过：创建 room、签发 publisher/viewer token、两个测试客户端加入、断开后 room status 变 `ended`；服务端拒绝非成员拿 token。 | `docs/architecture/09-realtime-message-live-implementation.md` §5 |
| M5.5 | 1:1 视频通话：`media.invite` 替代旧 P2P SDP 语义；单聊视频按钮创建 call room；被叫前台在线收到来电弹层；通话页全屏远端视频 + 角落本地预览 + 静音/摄像头/挂断；结束后写 `call_logs` 并追加 `kind=video_call` 消息。 | `tests/harness/livekit_call_quality` 通过：同局域网首帧 ≤ 2s，挂断后 conversation 出现 `[视频通话]` 消息；uiautomator / instrumentation 确认通话页远端视频容器、角落本地预览、静音 / 摄像头 / 挂断按钮存在且可点击。 | `docs/architecture/09-realtime-message-live-implementation.md` §6 |
| M5.6 | 第一视角直播：查验员创建 `live_session` 并发布后摄像头；协作页 `GET /v1/live-sessions?status=live` 展示在线视角；观看页订阅真实 video track；介入语音、标记预警、截图存档写 `live_annotations` 并推 WebSocket。 | `tests/harness/first_person_live_quality` 通过：viewer 收到非空视频帧，P95 延迟 ≤ 1500ms，标预警后 publisher 收到 `live.annotation`；uiautomator / logcat 确认协作页在线视角可进入，观看页已订阅真实 video track。 | `docs/architecture/09-realtime-message-live-implementation.md` §7 |
| M5.7 | 直播录制与回放：服务端触发 LiveKit Egress；webhook complete 后把 MP4/HLS 登记到 asset，写 `live_recordings.status='complete'`，推 `recording.ready`；协作页“近期录像”拉真实数据并可播放。 | `tests/harness/live_recording_egress` 通过：10s 测试直播在结束后 30s 内产出可播放 MP4，asset sha256 与对象存储一致。 | `docs/architecture/09-realtime-message-live-implementation.md` §8 |
| M5.8 | 实时协作观测闭环：所有关键日志带 `room_id/live_session_id/conversation_id/client_msg_id/server_seq/media_rtt_ms/packet_loss_pct/first_frame_ms`；harness `analyze.py` 输出正常 / 警告 / 异常 + 原因；更新 registry 中新增 Android 模块和服务依赖。 | `tests/harness/ws_message_order`、`realtime_message_sync`、`device_realtime_interaction`、`livekit_room_lifecycle`、`first_person_live_quality`、`live_recording_egress` 全部输出可判定结论；`docs/architecture/registry/modules.yaml` / `dependencies.yaml` / server registry 与实际模块一致。 | `docs/architecture/09-realtime-message-live-implementation.md` §9 |
| M5.9 | 语音消息转文字：采用自托管 FireRedASR2-AED + VAD/LID/Punc；服务端 `message_transcripts` 队列、ASR worker、`msg.transcript.updated` 推送、App 端气泡状态与重试入口落地。 | 无模型环境：发送语音后 payload 含 `transcript_status=pending`，`POST /v1/messages/{id}/transcript/retry` 返回 200；有模型环境：30 秒内语音 <= 2 秒返回，普通短语音平均 CER <= 5%，业务关键词召回率 >= 98%。 | `docs/architecture/09-realtime-message-live-implementation.md` §10 |

## M1 iHawk 接入

> 单设备路线：iHawk 自身 Color + Depth 双流，不接手机主摄。
> docs: `docs/architecture/01-depth-camera-integration.md`
>
> **当前 blocker**：M1.6 — Berxel Android SDK 内嵌的 libuvc-0.0.7 + 自家 libusb stack 在 4/5 测试机上 single-stream DEPTH 和 DUAL 都跑不通；只有 2510DRK44C 这个 BSP 特例稳。M1.2-M1.4 都要等 M1.6 完成才能在主流测试机上做完整验收（2510DRK44C 上可先并行推 M1.2/M1.3）。

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M1.2 | 暴露 iHawk Color/Depth 帧流：reader 线程把 `Frame.data` 拷成 `DirectByteBuffer`，Color YUYV 转 BGR888，按 `frameIndex` 配成 `RgbdFramePair` 并发到 `SharedFlow` 或 `Channel`。 | 真机连续 60s 输出 Color+Depth pair；pair 保留 frameIndex、timestamp、width、height、format；日志中 fps 稳定且无明显 GC 抖动。 | `docs/architecture/01-depth-camera-integration.md` §3.3 |
| M1.3 | 读取 iHawk 内参与注册参数：接 `getCameraIntriscParams` / `getDeviceIntriscParams`，打开并实测 `setRegistrationEnable`。 | `./dev.sh harness calibration_smoke` 通过；棋盘格 30/50/100cm 三组，深度投 color 边缘误差 ≤ 2 px。 | `tests/harness/calibration_smoke/` |
| M1.4 | Compose Color/Depth 实时预览：Color 贴 Surface，Depth 伪彩 Bitmap，HUD 显示 frameIndex / fps / timestamp / sync delta。 | `./dev.sh run` 后通过 logcat / uiautomator / instrumentation 确认 Color、Depth 预览节点存在，HUD 的 frameIndex / fps / timestamp / sync delta 持续更新。 | `feature/scan3d/Scan3dScreen.kt` |

### M1.6 Android UVC stack 重写（A 方案 path 2）

> 目标：用现代 libusb-1.0 + pupil-labs/libuvc 在 Android NDK 重写 Berxel Android SDK 的 native USB 层，替换 libuvc-0.0.7 + 自家 libusb stack。
> docs: `docs/architecture/10-android-uvc-stack-rewrite.md`
> 替换边界：53 个 JNI 入口函数（`Java_com_berxel_berxelInterface_api_admitmanager_BerxelHawkFunction_*`），Java jar 不动。
> **2026-05-26 Berxel 明确回复无支持精力** → 拿源码加速通道关闭，只走自 port 路径，工作量 1-2 个月。

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| ~~M1.6.0~~ | ~~联系 Berxel 索取 Linux SDK 源码~~ — **关闭 2026-05-26**：Berxel 回复无支持精力。只能靠自 port 突破。 | — | — |
| ~~M1.6.1~~ | ~~反编译 libBerxelUvcDriver.so 抽 Sonix XU cmd table~~ ✅ **核心架构完成 2026-05-26**：用 objdump 反汇编 `XU_Set_Cur / XU_Get_Cur / XU_Asic_Write / XU_I2C_Write` 链路，识别 Sonix 双轨协议（selector 0x01 ASIC reg I/O + selector 0x19/0x1e batch cmd + UVC standard probe-commit），硬编码 ASIC reg 0x10D0/0x10D8/0x10D9。NDK port 5 个 C 函数入口已定。具体 batch cmd payload schema 留 Phase 1 实施按需补完。报告 `.dev/m1.6.1-protocol-reverse/sonix-cmd-table.md`。 | ✅ | `.dev/m1.6.1-protocol-reverse/sonix-cmd-table.md` |
| ~~M1.6.2~~ | ~~Linux PC USB trace 抓 startStreams(DEPTH) 完整出包~~ ✅ **完成 2026-05-26**：bus7-master.pcap + bus8-companion.pcap 含完整 enumerate→XU vendor cmd→UVC probe-commit→BULK 序列；关键 Berxel Sonix XU 命令 wValue=0x1900/0x1e00 wIndex=768(Unit3/Interface0) 已识别。报告 `.dev/m1.6.2-usb-trace/report.md`。 | ✅ | `.dev/m1.6.2-usb-trace/report.md` |
| ~~M1.6.3~~ | ~~Phase 0 决定性预实验 — Linux PC 跑 Linux SDK sample 拿 ground truth~~ ✅ **完成 2026-05-26**：三组全绿（single-stream DEPTH 56ms 首帧 / DUAL 640 334ms / DUAL 1280×800@45 404ms + 84+ 秒满载稳定）。报告 `.dev/m1.6.3-pc-baseline/report.md`。彻底证伪 firmware/host BSP/mixed-bus/带宽 等假设，A 方案 path 2 锁定。 | ✅ | `.dev/m1.6.3-pc-baseline/report.md` |
| ~~M1.6.4~~ | ~~Android NDK 交叉编译主线 libusb-1.0 + libuvc~~ ✅ **完成 2026-05-26**：libusb-1.0.27（patched SONAME=libusb-1.0.so）+ libuvc 主线 fork，arm64-v8a + armeabi-v7a 4 个 .so 入 `third_party/libusb-android/` + `third_party/libuvc-android/`，含 README + 重建命令。vivo PD2324 真机 smoke test 走通 `libusb_set_option + init + get_version + exit` 全链路。 | ✅ | `third_party/libusb-android/README.md`, `third_party/libuvc-android/README.md` |
| ~~M1.6.5~~ | ~~Sonix XU 协议复现层~~ ✅ **完成 2026-05-26**：`native/berxel/{include,src}/gomob_berxel_protocol_sonix.{h,cpp}` 实现 5 个核心入口（xu_set_cur / xu_get_cur / asic_write / asic_read / batch_cmd），`tests/native_host/berxel_sonix_protocol_test.cpp` host test 6/6 PASS，字节序列（bmRequestType/bRequest/wValue/wIndex/wLength/data）100% 跟 USB trace ground truth 一致。 | ✅ | `tests/native_host/berxel_sonix_protocol_test.cpp` |
| M1.6.6 | Phase 2 — 实现核心 11 个 JNI 入口 + master XU 5 keepalive 协同（companion firmware 硬约束）；通过 `libusb_wrap_sys_device` + kernel HID/UVC driver detach 接管 Android UsbDeviceConnection.fd。当前进度（2026-05-27）：(1) JNI 全 11 入口完成；(2) `BerxelNativeStack.kt` 包装类入 `core:native-bridge`；(3) DI feature flag `BERXEL_STACK_BACKEND=SDK\|NATIVE_REWRITE` + `BerxelStackBackendOverride` runtime 覆盖 + 注入到 `BerxelService.start()` 让 backend 一翻就改路径；(4) `tests/harness/depth_singlestream/` harness 完整跑通。**真机 sweep 关键发现**（vivo PD2324 + kaMs ∈ {5,10,20,30,50,100,200,500} 全测）：master XU 5 keepalive 间隔跟 BULK 拿到字节**不相关**；所有组合都在 ~100ms 内死，最多拿 24×16384=393KB（<1 帧 513KB）。**结论**：M1.6.6 "≥100 帧无 null" 在 vivo PD2324 **不可达**，是 P100R3 firmware × vivo OTG BSP 的硬约束，详 `.dev/m1.6.6-jni-stack/findings.md` 2026-05-27 20:57 段。**剩余**：(c) 在 2510DRK44C（USB2 OTG 唯一通过 BSP）回归验收 NATIVE_REWRITE 路径 ≥10s 稳定流；(d) 评估 async transfer pool + 改 dwMaxPayloadTransferSize 是否在 vivo 上能突破 100ms 死线。 | 已达：feature flag 切换路径 + harness 完整链路 + 真机量化 vivo 死线。待补：2510DRK44C 上 NATIVE_REWRITE 拿 ≥100 帧无 null。 | `feature:scan3d` 集成；`.dev/m1.6.6-jni-stack/findings.md`；`core/native-bridge/.../berxel/BerxelNativeStack.kt`、`BerxelStackBackend.kt` |
| M1.6.7 | Phase 3 — 补完其余 43 个 JNI（exposure / gain / enable flag / 出厂参数读取等）；解码 P100R3 出厂参数文件 (params.bin / params100Q.bin / params137.bin)。 | 全部 53 JNI 跑通 Berxel SDK 原有 sample 应用所需路径；`harness scan_quality` 跟 2510DRK44C 上原 SDK 输出一致。 | — |
| M1.6.8 | Phase 4 — 把 `third_party/berxel-android/` 切到新实现：替换 libBerxelSdk.jni.so，Java jar 保留；ProGuard rules 更新；core/native-bridge 兼容层不动。 | `./dev.sh build` 出 apk；安装到 5 台测试机能 `BerxelService.start()` 成功；不影响 gomob 上层调用。 | `docs/architecture/01-depth-camera-integration.md` §3.5（更新） |
| M1.6.9 | Phase 5 — 5 台测试机完整回归矩阵：COLOR_ONLY / DEPTH_ONLY / DUAL (640×400 + 1280×800) 三种 stream type × 5 设备 = 15 组测试；`./dev.sh harness scan_quality` 通过。**强制接带 PD 供电 hub**（裸 OTG 直插不再要求跑通——见 [[finding_powered_hub_unblocks_vivo_dual_stream_2026-05-27]]）。 | 报告 `.dev/m1.6.9-regression/<deviceModel>.md` × 5；至少 4/5 设备 DUAL 640 拿稳 60s；DUAL 1280 在 USB3 OTG 机器上拿稳；订正 [[finding-p100r3-dual-endpoint-host-kill-2026-05-18]] 终结论。 | `tests/harness/scan_quality/` |
| M1.6.10 | **vivo uvcvideo 抢 master polish**（2026-05-27 记，非阻塞，遗留技术债）：vivo + powered hub 拓扑下 cold start 时 kernel uvcvideo 太快 bind master 0x0603，Android USB filter 把它从 UsbManager.deviceList 隐掉，inline test + NativeStack 都拿不到 master fd。当前 workaround = "接相机 → 立刻进 Sonix 页 → 立刻点测试"短窗口期；超出窗口必须物理 replug。彻底修需 BerxelService 加"持开但不 stream"模式让 inline test / NativeStack 借 fd，或研究 vivo USB filter rule 看能否绕过。 | (a) cold start + Dell hub 后 master 出现在 deviceList；(b) Sonix 页 inline test 连续跑 3 次都拿 ≥ 100MB / 0 错误。 | `feature:scan3d/SonixDebugScreen.kt`、`core/native-bridge/.../BerxelService.kt` |
| M1.6.11 | Linux host 自研 P100R3 SDK depth parity：在 `native/berxel/host` 复现原厂 depth/color/light-ir 双流与控制链路，不链接 Berxel `.so`。当前进度：确认 `DEPTH` 是 `13I_3D` fixed-point (`raw/8`) 而非散斑；`LIGHT_IR` 散斑流已复现到 `1280x800`、非零 100%、max 1023；新增 MJPEG/RAW assembler 和双流 demo。2026-05-28 关键修正：dense depth 不是 CPU `fillHole` 从 sparse 补出来，而是设备侧 `temporal_denoise=0` / `spatial_denoise=0` 触发；host SDK 默认已下发 `AE=1, confidence=3, temporal=0, spatial=0`，Qt6 demo 改为直接显示 active raw depth，processed depth 只作为可选后处理和 confidence mask 实验。新增 `tests/harness/berxel_depth_parity`：每轮先显式 sparse/dense 复位，采 vendor/host 多帧并输出相机自身噪声基线；2026-05-28 实机 PASS `.dev/berxel_depth_parity/`，host dense `1.0000`、vendor dense `0.9984`、host `--no-depth-controls` `0.1559`、median depth delta `1.19mm`、host-vendor median abs diff `37.125mm`，落在相机自身噪声底 `36.625..37.125mm` 内。harness 已自包含 vendor oracle 源码，并新增 `SCENE_*` 元数据、中心 ROI 稳定性指标和 `sweep.sh` 多材质/角度汇总。dual 同步 API 已补 `RgbdPairingStats`：stats 直接暴露 pair 数、drop/queue、last/mean/max host delta 和最后配对帧号；`pairs.csv` 写每对 start/end/midpoint、UVC PTS/SCR、FID 与 payload，`analyze_frame_csv.py` 输出 frame/pair 序号连续性。实机短测 `.dev/berxel-host-sdk/session-api-dual-sync-stats-20260528-232236/`：COLOR 39、DEPTH 82、pair 39，P95 delta `9.24ms`，退出 `stopped/duration_reached`。 | 继续补静态标靶真实采样和 Android 可移植 API；processed depth 仅作为 VIN/分割实验，不得替代 raw 量测；任何 depth 控制/assembler/pairer 改动都跑 `scripts/berxel-host-test.sh` 和 `./dev.sh harness berxel_depth_parity`。 | `native/berxel/host/README.md` / `native/berxel/host/docs/xu-command-map.md` / `native/berxel/host/docs/depth-pipeline-reverse.md` / `tests/harness/berxel_depth_parity/` |
| M1.6.12 | **Android 迁移 Step 1：portable 层抽离 + host bug 修复** ✅ **完成 2026-05-29**：把不依赖 libusb 的纯逻辑（assembler/pairer/depth&light-ir 解码/payload 生成/协议编排）抽到 `native/berxel/portable/`；引入 `IUvcDevice` 接口（A 方案，用户拍板），编排函数改吃 `IUvcDevice&`，host `UsbDevice : public IUvcDevice`；内部 `emit`→`log_line`（避 Qt 宏冲突）；修 oversized 帧元数据 stale + opening 期并发 stop 竞态两个 host bug。构建点改为 host SDK/probe/demo 都编 portable.cpp，纯逻辑 5 个单测改为 **只链 portable.cpp 不链 libusb**（编译期硬证明零依赖）。实机验收：host 单测 7/7、Qt demo build exit 0、depth parity status=OK 不退化。审核报告 `.dev/berxel-host-sdk/migration-audit-2026-05-29.md`。 | ✅ 已达：portable 层 libusb-free 编译+链接通过；三条验证全绿。 | `native/berxel/portable/README.md` / `docs/agent-memory/handoff_berxel_host_sdk_2026-05-29.md` |
| M1.6.13 | **Android 迁移 Step 2-3 代码完成 2026-05-29**：(Step2) portable.cpp 加进 `native/CMakeLists.txt`，跨 arm64/armv7 编进 `gomob_native.so`（NDK clang 零警告 + APK build SUCCESSFUL）。(Step3) 新增 `native/jni/berxel_dual_session_jni.cpp`：`AndroidUvcDevice : IUvcDevice`（libusb_wrap_sys_device 包 fd）+ 双会话驱动原样 mirror host 12 步序列（master XU5 replay→keepalive→companion XU3 replay→dense depth controls→UVC commit depth(0x82)/color(0x81)→bulk pump→RgbdFramePairer），全走 portable 层；JNI `berxelDualStart/Stop/Stats/PollDepthMm` + Kotlin `BerxelNativeStack.startDualNative` + `SonixDebugScreen` 按钮/`HARNESS_DUAL` broadcast。**实机验证（关键修正）**：先在 vivo 触发链路通但被 M1.6.10 master 不在 deviceList + port-disable 挡；**2026-05-29 在小米 2510DRK44C（双流唯一稳）实机跑通 depth-only**：完整 host 12 步序列在 Android 执行（master XU5 replay 246 + companion XU3 replay 7 + dense controls AE=1/conf=3/temporal=0/spatial=0 + UVC commit depth 640x401@45），bulk pump + portable assembler 出 **valid=1.000 稠密 depth、center_median≈423mm（raw/8.0）、depth_err=0、~26-40fps、keepalive ka=240**（对准 ~42cm 目标；对空旷处 valid=0 是场景非 bug）。**P0「depth 当 IR」实机修复确认**。修了一个真 bug：CMake `file(GLOB)` 不重 glob 导致新加 `berxel_dual_session_jni.cpp` 漏编（UnsatisfiedLinkError），已给所有 GLOB 加 `CONFIGURE_DEPENDS`。证据 `.dev/berxel-split/xiaomi-dual-success-2026-05-29.md`。 | ✅ Android 经 portable 链路出真稠密 depth，1:1 复现 host dense 行为。**待**：DUAL(depth+color) 验证；HyperOS 锁 input 注入，可加 manifest 常驻接收器实现零点屏 adb 驱动。 | `native/jni/berxel_dual_session_jni.cpp` / `.../BerxelNativeStack.kt` / `.dev/berxel-split/xiaomi-dual-success-2026-05-29.md` |
| M1.6.14 | **Android 迁移 Step 4（未开始）**：Kotlin 切到 `startDualNative` host 验证链路，删 `BerxelFrameAssembler.kt`/`BerxelMjpegAssembler.kt`/native-bridge 包内 `DepthFrame.kt` 重复实现，修 P0「NATIVE_REWRITE 把 depth 当 IR」（`BerxelService.kt:507-566`）、契约 `DepthFrame.data` 真为 16bit mm（去 jni_bridge.cpp:227 越界读 + fx/fy=0 除零），JNI 删异步 BULK pool 死代码；IR 预览改走独立 LIGHT_IR 流。 | depth 真出 16bit mm；录制/重建链路用真 depth；pair stats 持续更新。 | 审核报告 `.dev/berxel-host-sdk/migration-audit-2026-05-29.md` 第 2/4 节 |
| M1.6.15 | **深度时域降噪（路线 A，2026-05-29）**：设备关 temporal_denoise 换稠密(valid≈1.0)，代价是逐像素相邻帧抖动 ~38mm（远超 ≤1%@1-2m 规格），是量测/复现性头号敌人。portable 层新增 `P100R3TemporalFilter`（有界滑窗均值 N=8 + **自适应噪声门限** + temporal confidence），在 raw valid 像素上融合（量测真值），融合后再可选补洞。已完成：portable 模块 + host-test 7/7 + `depth_temporal_quality` harness + JNI 解析线程接入（cfg[13] 开关，默认开）。**真硬件验证（2026-05-29，服务器直连相机）**：固定门限不泛化（vendor 38mm/3.7× 但 live 63mm 退化 1.01× = EMA 陷阱）→ 改门限自适应 `max(45mm, k×median(|cur-est|), percent×Z)`，k=2.0；归一化后 vendor/host/live 三场景同一配置全 ~4.1×（vendor 4.08×、live 64.5→15.75mm/4.10×）、零偏移、密度不掉。**待**：Android 端 PollDepthMm live 路径设备视觉确认（portable 同源已硬件验证）。 | host-test 7/7 + harness OK + 真相机 live 序列 4.1×（已达）；Android 设备视觉确认（待）。 | `tests/harness/depth_temporal_quality/` / `.dev/depth-temporal-analysis/CONCLUSION.md` / `native/berxel/portable/gomob_berxel_portable.{h,cpp}` |
| M1.6.16 | **深度飞点剔除（路线 A，2026-05-29）**：结构光在前/背景断崖插值出的悬浮假点污染点云、是抖动 p95 主来源。4 视角设计评审（workflow）综合：grounding 证纯单帧检测过杀 24%，须 **三证合一**=时域不稳(stable_run/window_span，取自 TemporalFilter 窗口) ∧ 双侧角度超界夹心(step_max=tan(grazing)·Z/f·Δpx 物理坡度上界，半径外探抓薄带) ∧ 无共面支撑；在 **fuse 之后**做（fused 降噪后梯度更准）；保 raw 原值 + conf=0（删点由下游按 conf 跳过）；暖机只降权不删。已完成：portable `p100r3_flying_spatial_evidence` + `P100R3TemporalFilter::push(flying_mask)` 联合判定 + host-test 8/8 + `depth_flying_pixel` harness（合成 GT recall=1.0/geom_keep=1.0/纯几何 FP=0 + **vendor-dense 真实数据 removal=0.04% 不过杀**，对照单帧 24%）。**confidence 通道打通（2026-05-29）**：JNI depth_parser_loop 传 flying_mask 使飞点剔除在 live 生效（飞点 conf 置 0、fused 原值保留）+ 新增 `berxelDualPollDepthConf` poll 出逐像素 conf（修"算了从不 poll 出去"断链）+ Kotlin `dualPollDepthConf` 包装 + `DepthFrame.confidence: ByteBuffer?` 可选契约 + pull job 同帧（frameNumber 对齐守卫）填充 + `FrameRenderer.depth16ToBitmap` 按 conf==0 抹飞点。两 ABI 编译通过、conf 符号入 .so。**真硬件验证（2026-05-29 服务器直连相机 live）**：removal 0.05%、检测像素局部梯度中位 968mm vs 全图 29mm（精准命中真断崖、不过杀）。**待**：(1) Android 设备视觉确认预览飞点消失（UI 规范）；(2) **P1 下游加权**：3D 重建 `scanSessionIngest` 按 conf 加权/预剔（native 改 + A/B）；(3) 标定 blob 到位后用真 fx/fy+畸变重验角度上界；(4) 真机端侧帧预算实测。 | host-test 8/8 + `./dev.sh harness depth_flying_pixel` 全 OK + conf 通道全链路编译通过（已达）；真机预览飞点消失 + 重建按 conf 加权（待）。 | `tests/harness/depth_flying_pixel/` / `.dev/flying-pixel-analysis/{GROUNDING,SYNTHESIS}.md` / `native/berxel/portable/gomob_berxel_portable.{h,cpp}` |
| M1.6.17 | **深度"满屏噪点"根因+真置信+空间降噪（2026-05-29）**：用户盯实物报 demo 深度全是噪点。判据坐实是真深度（非 phase 误读/decode bug：低字节 256 distinct、直方图连续纵深、平滑区相邻差~1mm）但~74% 像素时域不稳（逐帧 MAD 中位 85mm，远超 ≤1%）。**根因**：(1) 密度优先（设备 temporal/spatial denoise 全关怼 valid≈1.0），(2) 设备 confidence 饱和废值（98.7% 标 255，噪声也满置信，任何 conf 掩码无效）。**解（用户选 A：真置信+保稠密）**：portable `P100R3TemporalFilter` 用窗口 `window_span` 派生稳定性置信替换废值（`confidence_from_stability`，span 大→低置信，即使窗口满）+ `apply_spatial_denoise`（median3 去脉冲→bilateral5 σr40mm 保边，fuse 后飞点前）。数据保稠密，下游按 conf 掩码/加权（契合多视角主线）。**离线真帧验证（30 帧 apply_filter）**：noise_p50 27→10.5mm、edge_keep 0.89、density 1.0；conf 不再饱和（<64 占 19%/≥200 占 24%），conf≥200 掩码后真表面干净浮现（24%，对齐离线 trust 实验）。host 测试 8 portable + sonix 6/6 全绿（flying harness 设 spatial_denoise=false 隔离）。demo 处理面板按 conf≥160 掩码出黑洞=测量级视图，编译通过。**静态复核完成(2026-05-30,相机恢复后 host 固定相机近目标 ~0.5m)**：**关键订正**——dense 静态噪声**不是**运动幻影,是真设备噪声。同一相机同一近目标:probe 手搓 dense 控制切不动设备(卡 sparse 13%,该 13% 强回波易像素时域 MAD 仅 0.1mm),**vendor SDK 复位切真 dense(99.8% valid)后,逐像素时域 MAD 中位 40mm / p90 148mm / MAD>30mm 占 56%——纯静态(固定相机)**。即 density-first 多出来的弱回波像素静态下就抖 ~40mm(对上 M1.6.15 vendor 38mm),**坐实 M1.6.15/16/17 打的是真静态设备噪声、IR 置信(弱散斑→噪)有物理依据**,非手持运动。附带发现:**自研 host probe 的 dense 控制 payload 在刚上电设备上不生效(只有 vendor SDK 复位能切 dense)——自研栈真实 gap,待查 probe dense 序列**(Android JNI 路径 M1.6.13 曾拿 valid=1.0,需确认是否同样依赖设备预热态)。**单帧误差实测(2026-05-30,对比 0.5% 官方标称)**:vendor 正常模式(denoise 开,官方标称模式)单帧时域 1σ **0.03%**(0.1mm@400mm,优于标称 ~15×)但**仅 11% 密度**(弱回波全丢);density-first(我们 100% 稠密)单帧 vs 真值中位 **8.9%**(34mm,p90 57%)、空间颗粒 6.4%(25mm)、≤0.5% 仅 28%——即 **0.5% 标称对的是单帧高置信稀疏像素;稠密图弱像素单帧就 ~9%,必须靠置信掩码(见 M1.6.19:conf≥80→0.05%)拉回**。**待**：(1) Android `FrameRenderer` 改按 conf 阈值掩码（非仅 conf==0）；(2) 多视角配准/重建按 conf 加权（接 M1.6.16 P1）；(3) 查 probe dense 控制为何切不动新上电设备。 | 离线真帧 noise 27→10.5mm + 真置信梯度化 + host 全绿 + demo 编译（已达）；真机静态复核 + Android 渲染掩码 + 重建加权（待）。 | `native/berxel/portable/gomob_berxel_portable.{h,cpp}` / `.dev/denoise-proto/` / `docs/agent-memory/finding_depth_noise_real_confidence_2026-05-29.md` |
| M1.6.18 | **修 master keepalive（USB 反复挂的真凶之一，2026-05-29）**：depth-only 模式跑 ~20min master 照样挂（dmesg `7-2.1 error -71` 掉枚举），真因=XU5 keepalive 的 `set_cur` 一直 `rc=-7 TIMEOUT`（~550ms 一次全失败，errs 累计 400+，ok 恒=1），firmware 拿不到 50ms 心跳→饿死；depth 靠 companion 照常推所以表面在跑。自供电+ganged hub 物理无法软断电恢复（[[finding_p100r3_master_hang_recovery_2026-05-29]]），只能物理断电。**2026-06-01 真机复现（2510DRK44C，M6.8b ④ 验证副产物）**：冷启动重插后 cameraOpenByFds 双流 setup 全绿（master color committed + companion depth committed frameSize=4101144=1280×801×4 + controls AE1/conf3/temporal0 + async bulk ep=0x82/0x81 submitted），但 master keepalive `set_cur rc=-7 TIMEOUT` ~550ms/次全败（ok 恒=1，errs 爬升），3000ms 无帧判死。**新对照证据**：同一 native 代码服务器 USB3 跑 parity valid=1.0、vivo+带电 hub 跑 219MB/0 错误均正常 → 2510DRK44C 上 set_cur 超时强指向【供电不足】（本机 SDK 路径仅 640×400 跑通过，NATIVE_REWRITE 硬编码 1280×801 功耗/带宽更大；setStreamProfile 在 NATIVE_REWRITE 被忽略无法降档）。**2026-06-01 续测：640 降档已排除带宽/功耗为 keepalive 主因**。把 NATIVE_REWRITE depth 默认改 640×401@45(≈23MB/s,UVC committed frameSize=1026584=640×401×4,稳在 USB2 内)后真机复测:`set_cur rc=-7 TIMEOUT` 依旧首次 ok=1 后全败、depth_chunks=0、**master color 0x81 也 0 帧（整机 setup 后失联）**。⇒ set_cur 超时**与分辨率/带宽无关**。**主假设(强)=供电不足 brownout**:决定性对照——**同一份 native 代码(同单 context + 同专用 event 线程 + 同步 keepalive)在开发服务器 USB3 跑 parity 全绿 valid=1.0**;若是 libusb 软件层事件锁争用 bug 服务器也该挂→软件设计没问题,挂在环境差(服务器 USB3+供电足 vs 2510DRK44C USB2+OTG 直插供电不足)。佐证:整机(master color 0x81 + companion depth 0x82 + master keepalive)在 setup 后**同时**失联=brownout 典型(NATIVE_REWRITE 三路并发起流峰值电流 > 手机 OTG 输出);[[finding_powered_hub_unblocks_vivo_dual_stream_2026-05-27]] 已实证带电 hub 救活 vivo 同一 NATIVE_REWRITE 双流(219MB/0 错误),生产 BOM 已列带电 hub。caveat:本机 SDK 路径 640 曾无 hub 跑通=SDK 起流时序/峰值更低,NATIVE_REWRITE 并发三路更猛。**次假设(弱)**:keepalive 同步 control vs 专用 event 线程争用(`gomob_berxel_portable.cpp:2003`),但被"服务器同码全绿"弱化。**下一步(按成本排序)**:① **2510DRK44C 串 PD/自供电 hub 重测**(最便宜+最高概率,armed 可一键复跑);② 仍挂再查 keepalive 异步化/独立 context。生产 BOM 选支持 PPPS 的带电 hub 以便远程软复位。 **2026-06-02 续:供电彻底排除 + 5 角度 workflow + 矛盾未决**。接外接电 Dell hub(`source_power=false sink_power=true` 手机被反向供电)后 640 双流**完全相同失败**→供电死透。`m1618-keepalive-rootcause` workflow 给【高置信假设】=master color bulk 0x81 饿死 keepalive set_cur(论据:服务器 parity 实为 depth-only `berxel_camera_host_probe ... 45 0`=enableColor=0)。**但被实机注释反驳**(`BerxelService.kt:443-450`:vivo+hub 成功是 color ON、vivo keepalive-only 100ms IO 死、HONOR master color commit 维持连接)。证据矩阵无 color 单变量规律(同 hub+color:vivo 活/2510DRK44C 死)→偏设备特定。**决定性测试(零码,待真机)**:2510DRK44C 跑 `--es stream dual --ez master_rgb false`(depth-only),`.dev/m68b-android/replug_depthonly.sh` armed。活→走 rank2(color URB `berxel_dual_session_jni.cpp:684` 48→8,host COLOR=0 不受影响);死→本机 BSP 级 keepalive(像 HONOR),换 vivo+hub 验 ④。不在矛盾假设上改码。 **★ 2026-06-02 决定性测试结果(2510DRK44C 直插)**:depth-only **活了**——depth_seq→302、valid=1.000、depth_chunks=5029、~422mm,15s 无掉线。**两条定论**:① **keepalive set_cur rc=-7 超时是红鲱鱼**:depth-only 下它照样全超时(ka_ok=1 ka_err 涨)但 depth 完美流(companion 0x82 独立于 master keepalive)→ M1.6.18 keepalive 超时**不挡深度扫描**,深度路径不需要修它;② **master color 0x81 是 2510DRK44C 双流真凶**(workflow rank1 确认):开 color 则整机 depth_chunks=0,关 color 则 302 帧。与 vivo/HONOR(color 保命)矛盾=**设备特定**。⇒ keepalive set_cur 修复优先级下调(深度不需要);若要本机 RGBD 双流,走 rank2 削 master color URB;否则本机 depth-only / 04b 时间复用单流。 | 根因定位（keepalive set_cur 全超时→master 饿死，已达）；set_cur 修复（待真机）。 | `docs/agent-memory/finding_p100r3_master_hang_recovery_2026-05-29.md` |
| M1.6.19 | **IR 引导深度精修离线验证（负面结论，2026-05-30）**：用户第一性质疑"交织 IR 占 40% 带宽不会是废数据，应能增强深度"。两轮反汇编坐实 SDK 里 `EdgeEnhanceInfraRed→inner_process_with_IR`(CannyEdge(IR)+region_fit) 是**零调用者死 API**，运行时深度链不碰 IR（三证：无 JUMP_SLOT / 无 lea / 无跨库 import；上层 dlsym 也无名字串）。复刻该算法做离线原型 harness `depth_ir_guided` 在真实交织 dump 量化：**0x0500 是结构光散斑帧**，Canny 检到散斑非物体边界 → IR 边缘对真边界(18 帧时域中值 Canny)F1 仅 0.25（去散斑 median9/open-close 后 recall 崩、F1 更低，救不回），远低于单帧深度边缘 0.88；留一法补洞 RMS IR 引导 328mm vs depth-only 73mm。**结论：维持 depth-only 精修(M1.6.15/16/17)，不接 IR 边缘引导**。**但 IR 作"置信"成立(2026-05-30 续测 confidence_probe)**：散斑局部对比度=深度可信度物理指标,真值用 18 帧时域 MAD,单帧"对比度低"预测不可信 AUC 0.82（控强度后仍 0.75、逐帧 0.76 极稳、空间反相关视觉确认）→ **IR 给零延迟单帧置信图**,补时域置信(需窗口+静态)的首帧/运动短板。**你第一性直觉正解:40% IR 带宽对深度有价值,价值在置信不在边缘**。 | 边缘:⛔（F1 0.25 vs 0.88）。置信:✅（AUC 0.82,对抗验证站得住）。**实现完成(2026-05-30)**：portable `p100r3_ir_speckle_confidence`(local-std/帧中值归一化→映射,曝光自适应)+ `P100R3TemporalFilter::set_prior_confidence` + push 稳定性块后 `conf=min(时域,IR)` 融合;JNI depth_parser_loop:IR 帧(0x0500)算置信 set、深度帧 push 融合;host 测试 +2(映射+融合)全绿(15 PASS),双 ABI APK BUILD SUCCESSFUL。**置信掩码恢复质量已验证(2026-05-30,mask_recovery.py 交织 dump)**:density-first 稠密单帧误差中位 0.25%(全体,场景相关;host 桌面场景达 8.9%)、≤0.5% 达标仅 57%;按 IR 散斑置信 **conf≥80 掩码:保留 49% 密度,误差中位降到 0.05%(=官方稀疏模式质量)、≤0.5% 达标 57%→78%**。即"稠密+置信掩码"在保留 ~50% 密度(比 vendor 正常模式 11% 稀疏稠 4-5×)同时把保留像素拉回标称精度,**验证 density-first+置信加权 > 稀疏干净 策略**(p90 尾 ~15% 仍在,叠加时域滤波更佳)。**host 重验闭环(2026-05-30,无需手机)**：确认这台 Linux host 厂商 SDK 直接能拿 depth+light-IR(三条独立 SDK 流,depth+light-IR 不能并发,但同 640×400 传感器逐像素对齐→静态顺序采集即可)。`host_capture.cpp`(SINGULAR 单会话 density-first depth 99.8% 稠密→light-IR pix=3 纯 IR 各 N 帧)+ `host_confidence.py` 复跑:**AUC 0.72–0.73、符号校验强成立(不可信像素散斑对比度 0.66 vs 可信 2.3)、掩码 conf≥160 保留 30% 密度单帧误差 7.9%→0.32%**。诚实差异:host 全裸 density-first 比 Android DUMP 更噪(单帧 ~8% vs 0.25%)需更高阈值,但结论一致——**Android 交织 / host 顺序两条独立路径互证**。订正旧"host 不交织 IR=异常":那是 SDK 解复用的正常行为。**待**：① Android `FrameRenderer` 按 conf 阈值掩码(非仅 conf==0);② 多视角配准/重建按 conf 加权落地(接 04b 主线)。 | `tests/harness/depth_ir_guided/`（prototype.py 边缘 + confidence_probe.py 置信 + mask_recovery.py 掩码恢复 + host_capture.cpp/host_confidence.py host 重验）/ `native/berxel/portable/gomob_berxel_portable.{h,cpp}` / `native/jni/berxel_dual_session_jni.cpp` |
| M1.6.20 | **端侧 native 重建按 conf 加权落地（2026-05-30，了结 M1.6.16-P1 / M1.6.17(2) / M1.6.19②）**：`DepthFrame.confidence` 此前止于预览渲染、不进重建；本条把它接进 native 重建(04b Stage3 端侧形态)。落地点(全部向后兼容可选 conf,nullptr=旧均权)：① `TsdfVolume::Integrate(...,conf)` 软加权累积 `w1=min(w0+conf/255,clamp)`+SDF 按权混合(conf=0 不贡献);② `IcpRegister(...,src_weights)` 加权 Kabsch 刚体拟合(低置信点降权防噪声拉偏位姿);③ `SessionIngest(...,conf)` 透传 TSDF + 经新 `DownsampleCloudConf` 与点云对齐喂 ICP;④ JNI `scanSessionIngest(...,confBuffer)`(零拷贝+capacity 守卫)+ Kotlin `confidence:ByteBuffer?=null` + ViewModel 传 `frame.confidence`。**权重单位语义**:开加权后 voxel.weight=累计置信(满置信观测为单位),min_weight=1.0=「≥1 满置信观测证据量」,有意更严(已文档化于 tsdf.h)。**对抗审查(workflow)驱动加固**:C2 单测改用真实 conf 权重(255/40 而非夸张 0.02)、harness 补系统性恒弱区场景。 | 公式单测 `conf_weight_test`(TSDF 加权/ICP 抗噪/向后兼容)5 组全过;行为 harness `scan_conf_weighting`:合成球面 **RMS 14.3→0.81mm(降94%)、内点 40%→100%、覆盖真球冠 98%**,系统性恒弱区**无空洞**(98% 覆盖,RMS 9.0→0.97mm),真硬件 chamfer 6.99→4.03mm(降42%);双 ABI APK BUILD SUCCESSFUL。**待**：(1) Android 真机视觉确认加权重建预览更干净;(2) **云端**多视角融合按 conf 加权(接 M3.14 Open3D worker,需预过滤低 conf 点/自定义权重)。 | `native/reconstruction/{tsdf,icp,scan_session}.{h,cpp}` / `native/depth/depth_projection.cpp` / `native/jni/jni_bridge.cpp` / `core/native-bridge/.../NativeBridge.kt` / `feature/scan3d/.../Scan3dRecordingViewModel.kt` / `tests/native_host/conf_weight_test.cpp` / `tests/harness/scan_conf_weighting/` |

## M1.7 内嵌相机形态验证（P0–P4）

> 方向：把 P100R3 从外接 OTG 改为焊进手机内部。**核心:内嵌真正只解决供电,并把另两个挂死从"物理拔电"降级为"GPIO 软件自愈";严禁把内嵌当挂死解药盲赌,先用最小成本验分水岭。**
> docs: `docs/architecture/11-embedded-camera-form.md`；原始四视角分析 `.dev/embedded-camera-analysis/raw-lenses.md`

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M1.7-P0 | **分水岭验证（最高优先,纯软件+小硬件,无需焊死）**：现外接 + GPIO 可控断电 carrier（或继电器/可控 PD-sink 模拟软断电），Linux host parity 台架。验三件事:① 关 color、RGB 走主摄后 master 还秒挂吗;② 供电稳 + XU5 心跳走实时线程（25ms 重发）后 set_cur rc=-7 是否消失、20min 饿死是否解除;③ "断电→上电→重枚举→重发 init/commit→重启 keepalive"恢复链可重入。先决:需 master 物理恢复（[[finding_p100r3_master_hang_recovery_2026-05-29]]）。 | ① color 关后 ≥30min 无 master 掉枚举;② 心跳实时化后 set_cur 连续 ≥30min 无 -71;③ 软断电后 ≥10 次重枚举+续流成功率曲线，断电时长 N 起步 2s。**这一步决定内嵌值不值**。 | `docs/architecture/11-embedded-camera-form.md` §11.5 |
| ~~M1.7-P2~~ | **✅ 作废（2026-05-29 订正）：无需自研结构光重建**。旧判断"companion 只推 IR-raw、depth 要移植 SDK 结构光重建"已证伪——dense controls 后设备 ASIC **直出 0x0600 metric 深度**(raw/8=mm)，与 IR/phase 帧交织在同一流；M1.6.13 已在小米实机经 native 链路取到 valid=1.0 稠密真深度。散斑→深度烘进 firmware DSP、不可导出也无需复刻。深度质量改走 depth-only 精修(M1.6.15/16/17)，IR 边缘引导已证无益(M1.6.19)。156B 内参 blob 接入并入标定线。 | 已无 showstopper：设备直出真 metric depth，host/Android 双端验证(M1.6.11/13)。 | [[finding_p100r3_depth_ir_interleaved_2026-05-29]] / `docs/architecture/11-embedded-camera-form.md` §11.6 |
| M1.7-P1 | watchdog 自愈闭环（同 P0 carrier）：三路独立判活（★用 master 心跳，绝不用 companion depth 帧）;DETECT_FAIL→断电 N 秒→重枚举→续流全自动;扫描会话把"相机重连"做成第一类事件（已采集数据保留、UI 不崩）。 | harness 打点触发原因/断电时长/重枚举耗时/恢复次数;注入故障后自愈成功率 + 会话不崩。 | `docs/architecture/11-embedded-camera-form.md` §11.3 |
| M1.7-P3 | 半内嵌 carrier：深度模组+主摄装同一刚性支架，板载 USB3 hub（INDIVIDUAL+真 PPPS）+ 板级电源树，host 仍外置/手机。 | hub 单口 power-cycle 隔离度（不误触 companion）实测;companion 5G SI 达标不出新 -71;一次性外参标定可行。 | `docs/architecture/11-embedded-camera-form.md` §11.3 |
| M1.7-P4 | 完全内嵌焊死（仅 P0–P3 全绿才进）：定制 FPC/载板 + 出厂标定工装 + 热管理（NTC 过温纳入 watchdog）。 | 外参覆盖温度区间标定;跌落/热循环后外参漂移 ≤1%@1-2m。 | `docs/architecture/11-embedded-camera-form.md` §11.5/11.6 |

## M1.8 多相机驱动抽象层提取（Berxel 不退化前置，host-first）

> 把 Berxel 从硬编码解耦成一个 driver，引入厂商无关 `IUvcDevice`/`ICameraDriver`/`ICameraSession`，为 eYs3D 留注册位。hardware-independent，靠 host 编译 + native_host 单测 + harness 回归守 Berxel 不退化。
> docs: `docs/architecture/12-camera-abstraction.md`

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M1.8.1 | `native/camera/` 通用件迁移：`IUvcDevice`/`UvcRawFrameAssembler`/`UvcMjpegFrameAssembler`/`RgbdFramePairer`/`negotiate_uvc_stream`/`replay_xu_payloads`/`TemporalFilter` 从 `native/berxel/portable` 迁 `native/camera`，命名空间 `gomob::camera`，`FlyingPixelConfig` 参数化（去 P100R3 前缀）。Berxel include 改路径，签名零变更。 | 解耦点 🟢#1-4 + 🟡#5 完成；`scripts/berxel-host-test.sh` 全 PASS（不依赖相机）；`./dev.sh harness berxel_depth_parity` status=OK 不退化；`./dev.sh build` APK SUCCESSFUL（双 ABI）。 | `docs/architecture/12-camera-abstraction.md` §1.1-1.2 |
| M1.8.2 | `camera_session.h` 接口落地：`ICameraDriver`/`ICameraSession`/`CameraCapabilities`/`CameraFrame`/`DepthControls`/`StreamProfile`，`gomob_camera_core` STATIC target 进 CMake。 | 接口头 + target 编译过；`CameraCapabilities` 能正确描述 P100R3（metric/ir/confidence）；host 单测 link `gomob_camera_core` 通过。 | 同上 §1.1 |
| M1.8.3 | `BerxelDriver` + `BerxelSessionAdapter`：包装现有 `P100R3DualSession` 实现 `ICameraSession`，`process_p100r3_depth_frame` **零改动**。 | host demo（`run_berxel` 或等价）经 `ICameraSession` 跑通双流；`./dev.sh harness depth_temporal_quality` stability_gain ≥4× 不退化；`berxel_depth_parity` 仍 OK。 | 同上 §1.4 |
| M1.8.4 | JNI 通用化：新增 `native/jni/camera_session_jni.cpp`（`cameraOpen/Poll/SetControls/Stop`），marker 判别/raw→mm 下沉 `BerxelDriver`；旧 `berxelDualStart` 兼容壳转调。 | 解耦点 🔴#9-12（`berxel_depth_parity` 真机回归零退化）；2510DRK44C 真机经 `cameraOpen` 双流 ≥100 帧无 null。 | `native/jni/camera_session_jni.cpp` |
| M1.8.5 | `core:native-bridge` `CameraModel`/`CameraSessionFactory`/`CameraDriver` + `BerxelNativeStack` 常量参数化为 `CameraDeviceProfile`。 | 解耦点 #13-20；`./dev.sh build` 出 apk；2510DRK44C 经 `CameraSessionFactory` 走通；VM 层无改动。 | `core/native-bridge/.../CameraSessionFactory.kt` |
| M1.8.6 | feature:scan3d 自动识别 + 类型展示：`deviceTypeLabel` 贯通，UI 读 driver 档位。 | 解耦点 #21-23；真机 UI 显「Berxel iHawk」；uiautomator dump 验标题/badge；无崩溃。 | `feature/scan3d`；`docs/architecture/01-depth-camera-integration.md` |

## M6 eYs3D/Etron 自研 UVC 驱动（全自研，不引原厂 SDK，host-first → portable → Android）

> eYs3D 一路到底含深度。**实测 RS-D550 `0x3438:0x0206` SN BF301208**（2026-06-01 host 验机；非逆向猜的 0x1E4E），标准 UVC 1.00 单 USB 设备双 VS interface：IF1 BULK 0x81 = L+R 立体（YUY2 2560×960/MJPEG）、IF2 BULK 0x82 = 第二路（YUY2 16bpp，疑似深度/视差，待 M6.2 实测）、IF0 含 Etron XU id=4 GUID {c2b1ccad-…feec}。**深度端侧软件重建或读 IF2 on-chip 深度**（自研对标 libDepthMSR 但不链）。host-first：Linux 服务器 libusb 直驱，核心进 `native/eys3d/portable`，再移植 Android JNI。带电 hub 供电。
> docs: `docs/architecture/13-eys3d-driver.md`（待 M6.1 验机后建）、`tests/vincreator-apk/REVERSE-ENGINEERING.md`、`docs/agent-memory/finding_vincreator_eys3d_uvc_blueprint_2026-06-01.md`

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| ~~M6.1~~ | host 验机 ✅ **完成 2026-06-01**：服务器（带电 Genesys hub，USB2）枚举到 **RS-D550 `0x3438:0x0206` SN BF301208**，`lsusb -v` dump 完整 descriptor。标准 UVC 1.00 复合设备：IF0 VideoControl（含 **Etron XU id=4 GUID {c2b1ccad-abf6-48b8-8e37-32d4f3a3feec} 5 ctrl** + PU + 2×Camera Sensor 输入终端 + 状态 int 0x83）；**IF1 VideoStreaming BULK 0x81 512B**（terminalLink=2/sensor1，Format1 YUY2 16bpp 2560×960@30(L+R)/1280×480/640×480/640×240/320×240，Format2 MJPEG 9 档含 2592×1944）；**IF2 VideoStreaming BULK 0x82 512B**（terminalLink=7/sensor2，Format1 YUY2 16bpp 1280×960/1280×256/640×480/640×128/320×240，疑似深度/视差）。两路均标准 UVC-over-BULK → `negotiate_uvc_stream`+`UvcRawFrameAssembler` 可复用。uvcvideo 已绑（/dev/video2-5），自研驱动需 detach。**SN BF301208 对应 VINCreator `VIN_BF301xxx.bin` 标定族**。 | ✅ descriptor 全量入 `13-eys3d-driver.md` §1。待 M6.2 实测确认 IF2 是深度还是 raw 第二目。 | `docs/architecture/13-eys3d-driver.md` §1 |
| M6.2 | 自研 UVC 取流：`EYS3DUsbDevice : IUvcDevice`（libusb）+ 复用 `negotiate_uvc_stream` + `UvcRawFrameAssembler` 组装单流。**自研路径已验证 2026-06-01**：`native/eys3d/host/eys3d_probe.cpp` 编译通过，对 RS-D550 标准 UVC probe/commit + SET_INTERFACE 全部成功（maxFrame/maxPayload 正确协商），IF1/IF2 × YUY2/MJPEG × 多分辨率均试过。**但 bulk_in 全 -7 TIMEOUT、0 帧**。**分水岭测试坐实**（`eys3d_v4l2_probe.cpp`）：系统标准 `uvcvideo` 驱动 STREAMON 后同样 0 帧 → **非 probe bug，是 RS-D550 默认不推流，必须先发 Etron 专有 XU 激活序列**（对应 VINCreator `ApcCamera.open()→startCameraViaDefaults`）。⇒ 卡在 M6.3。 | 自研取流流程正确性已证（协商/SET_INTERFACE）；首帧待 M6.3 激活序列。 | `native/eys3d/host/eys3d_probe.cpp`；`tests/harness/eys3d_singlestream` |
| ~~M6.3~~ | **✅ 完成 2026-06-01：自研 libusb RGBD 双流首光（零厂商 SDK）**。`eys3d_replay_stream.cpp dual` 跑通 **color 32 帧/1228800B + depth 16 帧/2457600B 并发出流、5.6MB/s、drops=0**。关键链路：① **RHEL9 能出流**（eSPDI 直调 color/depth 各 60/60，推翻"环境不兼容"，旧 trace_7u.txt 0-BULK 是交互菜单没驱动真取帧）。② **完整开流序列解码**（usbmon `sdk_stream_trace.txt` 45 笔 SET_CUR）：XU(unit4 `wValue=0x0300 wIndex=0x0400`)写 `82 f0 14`/`20 f0 02`/`20 ed 00` + selector0x0a 计数器握手（旧 0xE0/0xE3 猜测作废）；标准 VS PROBE→GET_CUR→COMMIT（IF1 `fmt1 frame4 maxPayload2048`、IF2 `fmt1 frame1 maxPayload3072`）；无 SET_INTERFACE；COMMIT 后~1.5s 暖机。③ **取流 4 硬约束**：异步多 URB(timeout=0) / 回调只 memcpy 不组装 / URB=maxPayloadTransferSize 一 URB 一 payload / **depth 仅在 color 并发排空时才出流(共享流水线，单读 ep82 即 STALL)**；assembler `drop_partial_on_uvc_header=false`。④ 硬件：内置 hub 线材差强制 USB2；约 20min 自动关机，长按开机。 | color 已证真实低光场景图（eSPDI 同解法验证 YUYV 1280×480 格式对）；depth=1280×960 u16 容器 ~50% 非零。**遗留**：自研帧 size 切产生水平条纹（payload 剥头/边界 bug，raw dump 分析中），eSPDI 同格式渲染干净。 | `native/eys3d/host/eys3d_replay_stream.cpp`；`.dev/eys3d-sdk/{eys3d_sdk_stream.cpp,sdk_stream_trace.txt}`；`docs/agent-memory/finding_rsd550_open_sequence_decoded_2026-06-01.md` |
| ~~M6.3b~~ | **✅ 完成 2026-06-01：FID 法拆帧修条纹**。RS-D550 是标准 UVC bulk，每 payload 带 12B UVC 头，**按 FID(bit0) 翻转切帧**（Berxel 的 size 切+UvcRawFrameAssembler 不适用）。实测每帧恰 1228800B(603×2048+1×1104 data+1×12 EOF头)。剥头 FID 切后 **color 渲染出干净双目立体对**（L/R+基线视差，真实低光场景）。会话开始先 `libusb_reset_device` 清残留(否则连续开关卡 STALL)。color+depth 各 26 完整帧/1228800B。 | color 验证无条纹真图像；depth 同样 26 帧 1228800B 稳定。 | `native/eys3d/host/eys3d_replay_stream.cpp` |
| ~~M6.3c~~ | **✅ 完成 2026-06-01：正式 `Eys3dUsbDevice`/`Eys3dHostSession` 接 ICameraSession**。`native/eys3d/host/eys3d_usb_device.h` 提炼共享 `Eys3dUsbDevice:gomob::camera::IUvcDevice`(replay_stream 已 refactor 复用,编译回归过);`Eys3dHostSession:ICameraSession`(libusb 传输壳:reset+claim IF0/1/2 + 逐字 proven 开流序列 + 双端点异步 32 URB + 单线程 FID 增量组装 → 喂 `Eys3dSessionCore`);`UsbContext` host(默认 context)。`scripts/eys3d-host-test.sh` smoke + replay 仅编译均过。 | ✅ smoke 经 `ICameraSession` 构造/委托;replay 逻辑无回归(编译)。出帧验证设备门控,见 M6.5。 | `native/eys3d/host/`；`docs/architecture/12-camera-abstraction.md` |
| M6.4 | **端侧自研 stereo 深度**。**★2026-06-01 重大订正**：旧"IF2 因 flash 读 ZD 表失败而死"是误判(逆向证 flash/ZD 读全成功，`FW_UNKNOWN_STRUCT_LEN_OF_STI` 是 flash 保护探测非致命错误，不在深度路径)。**逆向定论**(6-agent workflow，`docs/agent-memory/finding_rsd550_open_sequence_decoded_2026-06-01.md`)：视差 100% 来自设备 ASIC(IF2)，eSPDI **零软件 stereo**(内嵌 OpenCV StereoBM/SGBM 是死代码)；`Z_mm=byteswap(ZDtable[视差×8])`(2048项LUT) 或几何 `Z=fx'·B/disp`；IF2 出 live 视差前置=**点亮 IR 投射器(FW 0xE0,SetIRMaxValue6+SetIRMode0x03+SetCurrentIRValue3)+AE 曝光+11bit 模式**。**SDK 实测**：IR armed 后深度仍列恒定竖条纹(列std41/行std20830 非物理)，ASIC 没真算视差；唯一未验 arming=AE(设备关机没跑成)。⇒ **双轨**：(主线)端侧软件 stereo——IF1 1280×480 双目是真立体对(已验视差)，自研 rectify+SGBM→视差→Z；(待验)硬件 ASIC 深度需补 AE arming。**①✅ 标定+ZD表已提取**：左目 fx=1292.674/cx=637.946/fy=1292.305/cy=496.661+dist、矫正 fx'=1229.205/cx648/cy482.865/B49.98mm 在 `rectlog_1.bin`/`rsd550_calib.json`；ZD表(视差→mm,byteswap)`tables/zd_dt4_b4096.bin`、rectify表`tables/rectify_{0..3}.bin`。**②✅ 原型跑通**(`.dev/eys3d-probe/stereo_depth_proto.py`)：cv::stereoRectify(左K+R≈I+水平基线)+SGBM+Z双路；**几何Z与ZD表查表一致(±5%)→度量换算已验证**。**遗留**：右目内参/R/T 在 rectlog 结构体被"32"污染解不出(暂用左右等内参近似)；暗/无纹理场景 SGBM 仅~10%有效、矫正 dy 不收敛→**③④ 需明亮纹理场景调矫正+验精度(下次开机)**。 | 已知距离平面/盒子深度连续、误差≤1%@1-2m(对标 P100R3 规)；harness `analyze.py` 判定。 | `.dev/eys3d-probe/stereo_depth_proto.py`；`.dev/eys3d-sdk/{eys3d_sdk_depth.cpp,extract_tables.cpp,tables/}`；`native/eys3d/host/eys3d_stereo_depth.cpp`(待落)；`tests/harness/eys3d_disparity` |
| M6.5 | **下次设备开机验证清单**(设备~15min硬定时关机)：① **★mode25 复验硬件深度**(最高优先)——`.dev/eys3d-sdk/run_mode25.sh` 一键:color 1280×256 MJPG@5+depth 640×128@5+type36+IR,看 depth u16 是否回 0-2047(之前用错模式 1280×480@30 → ASIC 退化吐 raw=列恒定垃圾)。② 抓明亮**非周期**纹理场景(规则点阵/钢板对 SGBM 病态)→ 喂 `stereo_depth_proto.py` 调矫正+验精度。③ 重试 `APC_GetRectifyMatLogData` 4096B 拿干净右目内参/R/T。④ **mode25 arming 已离线锁定**(靶向 RE workflow,2026-06-01):videoMode 写 0xF0 = **36(0x24)**(jadx `CameraModeKt DEFAULT_ROSIE4_U2_MODE.videoMode=36`),已落 `Mode25Usb2Plan()`(videoMode=36 + color 1280×256 MJPEG@5 + depth 640×128@5),fd/host 会话默认切到它。**仅剩流协商细节真机一抓即定**:VS bFrameIndex(1280×256_MJPEG/640×128,`lsusb -v` 解析)、depth 传输帧是否含状态行(frame_bytes)、**AE arming**(`APC_SetCTPropVal(CT_AUTO_EXPOSURE_MODE=0,8)` 是否 ASIC 进 live-depth 最后一环,上次关机没跑成)。⑤ ZD 表/补偿 pars 从设备 flash pull 注入(per-device 标定,现用几何兜底)。 | mode25(videoMode=36)出真深度(u16∈0-2047,列内 std 不恒定)；好场景 SGBM>60% 且 dy≈0；已知距离误差≤2%；`Eys3dFdSession`/`Eys3dHostSession` poll 出 metric depthMm。 | **`tests/harness/eys3d_mode25/`(自研零SDK一键:lsusb解析→流→判定)**；`scripts/eys3d-parse-descriptor.py`；`native/eys3d/host/eys3d_mode25_stream.cpp`；`.dev/eys3d-probe/stereo_depth_proto.py` |
| M6.6 | **eYs3D 双相机 portable 地基(✅ 已落码+单测 2026-06-01)**。架构定:深度**硬件ASIC主+软件stereo兼**双路。落码:`native/camera/`(`camera_device.h` 再导出 IUvcDevice/XuPayload/协商/组装件到 `gomob::camera` 零改 Berxel;`camera_session.h` 新增 ICameraDriver/ICameraSession/CameraCapabilities/CameraFrame/DepthControls);`native/eys3d/portable/`(`eys3d_depth` ZD表 byteswap+查表→mm;`eys3d_protocol` XU arming SetVideoMode36/interleave/IR;`eys3d_driver` 能力构造+DepthFinalizer)。4 单测全过:`eys3d_depth_test`(对真实ZD表 512→502/1024→251/2047→125)、`eys3d_protocol_test`(wire字节)、`camera_abstraction_test`、`eys3d_driver_test`。 | `scripts/native-host-test.sh` 全过零回归。 | `native/camera/`、`native/eys3d/portable/`、`tests/native_host/eys3d_*` + `camera_abstraction_test` |
| M6.7 | **eYs3D host driver 串联(✅ 主体已落码+单测 2026-06-01;出帧验证设备门控)**。落码:`Eys3dDriver:ICameraDriver`(match 0x3438:0x0206,capabilities=`BuildRsd550Capabilities`,open_host 用 `UsbContext` 开设备出 `Eys3dHostSession`,open_fd→Android 留 M6.8);`eys3d_depth_router`(IF2 11bit 视差帧→剥状态行/字节序→`DepthFinalizer` 查表-or-几何→metric depthMm);`eys3d_session_core`(传输无关会话引擎:arming 装配+深度路由+帧队列背压+serial+状态机+统计,可单测);协议补全(`MakeFwRead`/`MakeCounterTick`/`MakeSetStartTrigger`,**videoMode 寄存器值(proven 0x02)与 depthDataType(36)解耦**——原 `MakeSetVideoMode(36)` 把两者混了)。3 新单测全过(`eys3d_depth_router_test`/`eys3d_session_core_test`/`eys3d_host_session_smoke` libusb)+ 协议测试更新。**设备门控遗留(并入 M6.5)**:实际出帧 + **mode25 正确开流值**(videomode_reg/PROBE 分辨率/depthType36 线编码)——当前 `ProvenWrongModePlan` 是 1280×480 错模式占位(出列恒定垃圾),换 mode25 真值即出 apk 级深度;depth ROUTING(core)已对,只设备 ASIC 跑错模式。 | ✅ `eys3d-host-test.sh` smoke 经 `ICameraSession` 出 depthMm 契约;真机出帧 `u16∈0-2047` 待 M6.5。 | `native/eys3d/host/`、`native/eys3d/portable/eys3d_{depth_router,session_core}.*`、`native/camera/host/`；`docs/architecture/13-eys3d-driver.md §2bis` |
| M6.8a | **✅ 软件 stereo fallback 进 native(2026-06-01)**。`eys3d_stereo_depth`(自研 SAD block-matching,避 OpenCV):窗口 SAD + 唯一性比 + LR 一致性 + 纹理门 + 抛物线亚像素 → 视差×8 → 复用 `DepthFinalizer`(ZD 表/几何)出 metric depthMm;`SplitSideBySideYuyvToGray` 拆 IF1 并排 L/R;双路经 `core.OnDepthMmFrame`(路径无关入口)汇同一 depthMm 队列。`eys3d_stereo_depth_test` 全过(合成已知视差 D 恢复恰=D×8 >95%、几何 Z 与 ZD 表度量、亚像素、平坦图判无效、YUYV 拆分)。**真机矫正质量**(右目内参/R/T 污染、非周期纹理场景)待 M6.5。 | ✅ `native-host-test.sh` 全过零回归。 | `native/eys3d/portable/eys3d_stereo_depth.*`；`docs/architecture/13-eys3d-driver.md §3` |
| M6.8b | **JNI 双相机统一入口 + Kotlin 栈(✅ ①②③④⑤ 主体全落+全链编译/单测 2026-06-01;仅剩 eYs3D/Berxel 真机出帧门控)**。落地计划见 workflow `m68b-integration-map`。①✅ `CameraRegistry`(按 vid:pid 分发)。②✅ **`RunEys3dStreamLoop` 抽取**(`native/eys3d/host/eys3d_stream_loop.*`,host 与 Android-fd 两壳共调,零重复;`Eys3dHostSession` 改调它不腐化)+ **`Eys3dFdSession`/`Eys3dFdDriver`**(`native/eys3d/android/`,`libusb_wrap_sys_device(fd)`→共享循环,坏 fd→kError 优雅降级,几何度量默认注入)。③✅ **JNI `camera_session_jni.cpp`**(`cameraOpenByFds/Stop/PollDepthMm/PollColor/Stats/SetControls/CapabilitiesJson`,全局 registry 注册 `Eys3dFdDriver`)+ **`ICameraSession::snapshot_depth_mm/color`**(最新帧 consume-once,两相机统一契约)+ **`NativeBridge` external fun** + **`CameraStack`/`CameraModel`**(Kotlin 编排+判型,`CameraModelTest` 过)+ **`usb_device_filter.xml`** 加 0x3438:0x0206。`native-host`+`eys3d-host`+`dev.sh build`(arm64+v7a `.so`)+`dev.sh test` 全过。④◑ **Berxel→`ICameraSession` adapter(2026-06-01 生产全量迁移,host 编译+Hilt 验证,真机复验门控)**:✅ 扩 `ICameraSession`(加 `snapshot_confidence`/`snapshot_ir`/`extended_stats(16-long)`/`dump_raw_depth`,默认空,eYs3D 不受影响)+ ✅ `berxel_dual_session_jni.cpp` 抽 `berxel_open_dual` + `berxel_snap_{depth_mm,conf,ir}`/`berxel_take_color`/`berxel_get_stats`/`berxel_dump_depth` core(legacy JNI 逐字拷贝)+ ✅ `BerxelSessionAdapter`/`BerxelDriver`/`MakeBerxelDriver()`(`native/berxel/host/berxel_camera_adapter.h`)+ ✅ `camera_session_jni` 注册 BerxelDriver + 4 新 JNI(`cameraPollDepthConf/IrGrey/ExtendedStats/DumpRawDepth`)+ ✅ Kotlin `BerxelNativeStack` 全量迁到 `cameraOpenByFds(0x0603:0x001f, [masterFd,companionFd], packed options_json)` + `cameraPoll*`/`cameraExtendedStats`/`cameraStop`(options 打包/解包小端对称)。**双 ABI APK + native-host + eys3d-host + 全模块 Hilt + debug/release 单测全过**。**不退化**:Berxel 每条通道(depth/conf/IR/color/16-long stats/dump)逐位保留;legacy `berxelDual*` JNI + NativeBridge external fun **冻结保留为 device-gated 回退**(生产已不调用,真机复验 camera* 通过后整段删)。**真机门控**:vivo+带电 hub 跑 cameraOpenByFds 双流 ≥100 帧、depth raw/8 mm 合理、conf/IR/stats 与 legacy 一致 → 通过后删 legacy。**2026-06-01 Android 真机验证到 firmware 边界（2510DRK44C，USB2）**:APK 装上(MIUI 限制用 push+pm install 绕过);debug intent(`DEBUG_BERXEL_START`)实证生产路径走迁移后链路——`★ backend=NATIVE_REWRITE` → `BerxelNativeStack.startDualNative`(=cameraOpenByFds);冷启动重插后**两 fd authorized=true 全开 + berxel_setup_and_launch ok**(master color UVC committed + companion depth committed + controls + async bulk ep=0x82/0x81 submitted)——迁移后 JNI 全链路打通到 setup_and_launch。**撞墙点=M1.6.18 master keepalive `set_cur rc=-7 TIMEOUT`→0 帧,与 legacy 同行同墙(keepalive 在逐字拷贝的 native core 里,迁移零改动)**。⇒ **不退化成立(执行路径逐步一致+撞同一已知 firmware 墙);frames 门控被 M1.6.18 阻(供电/降档,与 ④ 正交)**。legacy 仍冻结,frames+conf/IR/stats 比对待 M1.6.18 解后补。**★ 2026-06-02 Android depth 路径真机 PASS(2510DRK44C 直插 OTG,无 hub)**:`--es stream dual --ez master_rgb false`(cameraOpenByFds → BerxelDriver dual session,enableColor=false)实测 **depth_seq 持续涨到 302、valid=1.000、center_median≈422mm(raw/8 合理)、depth_chunks=5029、depth_err=0、q_drops=0**,15s 无掉线。⇒ **迁移后 cameraOpenByFds 在 Android 真机产出真实 metric 深度,④ 深度路径(承载 conf/IR/stats 的核心)device-validated**。关键澄清:keepalive set_cur rc=-7 超时**是红鲱鱼**——它照样全超时但 depth 不受影响(companion 0x82 独立推);M6.8b ④ 深度不退化已成立。**剩 RGB color**:master color 0x81 在 2510DRK44C 上致命(开则整机 depth_chunks=0)。**2026-06-02 rank2 证伪**:master color URB 48→8 真机复测**照样 depth_chunks=0 整机死**(`ep=0x81 count=8` 已确认生效)→ **不是 URB 数量,是并发 master color 流本身与 companion depth 在该机 USB stack 互斥**。已撤回 rank2(恢复对称 48)。⇒ **不是 URB 数量问题**。**★ 用户订正(2026-06-02):原厂安卓 SDK 能同时拉通 COLOR+DEPTH** → 该硬件【支持并发 RGBD】,是我们 NATIVE_REWRITE 的并发实现有 bug,非设备极限。原厂 SDK = oracle。**待 RE 原厂 SDK 并发 color+depth 做法**(候选差异:① 我们的 XU5 keepalive set_cur 一直 rc=-7 超时,在 master fd 上与 master color bulk 并存时可能把 master 搞挂——depth-only 无 color 故 companion 独活;原厂可能不做这 keepalive 或做对了;② master color 端点 bulk vs isoc;③ 流启动时序/alt-setting)。**先验最可疑的 keepalive 假设**:color+depth 但关 keepalive(keepaliveMs=0)看是否救活。HONOR(JDY_AN00)做对照失败:USB-C 策略锁死,带电 hub→手机进 UFP/充电模式不当 host(`set-port-roles` 打不过去),直插供电不足。下一步:depth-only 下比对 cameraOpenByFds vs legacy 的 conf/IR/16-long stats → 一致即删冻结 legacy。**★★★ 2026-06-02 MIX 配方 definitive 抓到(`tests/harness/berxel_mix_trace/`,服务器 usbmon 抓原厂 SDK `setStreamFlagMode(MIX)+startStreams(COLOR|DEPTH)`,gotColor=20/gotDepth=19 成功)★★★**:完整原厂 MIX master XU5 序列(21 条)已存 `core/native-bridge/src/main/assets/berxel/iHawkP100R3_master_mix_init.json`。**关键订正我之前 2 个 patch**:① StreamFlagMode(reg 0x0030)在 MIX 下值=**`0x0000`(b10=00 b11=00)写两次**——我 patch 的 0x02 错了(enum MIX=0x02 但线上编码=0x00);② 多一条 **`cmd0x0007=0x0001`**(夹在两次 StreamFlagMode 之间)我们没发;③ COLOR OpenStream(640×400@30,`...90011e00`)在 StartUsbStream(#12 `02000600`)+HostTime 之后、StreamFlagMode 之前(中段位置我猜对了);④ 原厂 MIX master 全程仅 21 条 vs 我们 replay 246 条 SINGULAR init。**★★★ 2026-06-02 MIX 实现 + host 真机 PASS(definitive,camera 在服务器)★★★**:① 从 pcap 还原原厂 UVC commit 参数(零猜测)——**master color = fmt1 frame3 interval 333333(30fps)**、**companion depth = fmt1 frame2 interval 222222(45fps)**(addr57=master/addr5=companion 经 XU wIndex 反查确认)。② companion MIX 序列(8 条)落 `iHawkP100R3_companion_mix_init.json`,相对 depth-only 仅 cmd#5 reg0x19 `01→04`(MIX 模式寄存器)+ 末尾多 `0102 00`。③ **撤掉错 patch**:删 `p100r3_mix_flag_mode`/`patch_p100r3_master_stream_flag_mode`(0x02 反编译错推断,host MIX trace 证实线上=0x0000,已 baked 在 MIX 资产);保留 `patch_p100r3_master_color_open_stream_payloads`(就地重写 OpenStream 保证与 UVC commit 一致,host 实测重写 hex == 原厂 `42580c00...011e00`,no-op)。④ **wiring**:Kotlin `startDualNative` 按 enableColor 选 MIX vs SINGULAR 资产 + color 档改 30fps(640×400@30,interval 333333);native setup_dual 去 StreamFlagMode patch;host_sdk `master_payload_path`/`companion_init_path` + host_probe 默认 color+depth 自动选 MIX 双资产。⑤ **host NATIVE_REWRITE 路径实测(= Android 同 portable 同资产同序列)**:裸 `--color --depth` 自动选 MIX → **color 1040~1086 帧 / depth 146~147 帧(metric 中心 298mm 有效率 1.0)/ RGBD 145~147 对 / keepalive 100/100 ok / 全程 0 错**。⑥ 建 harness `tests/harness/berxel_mix_replay/`(run.sh + analyze.py,可判定 ✅正常/⚠警告/❌异常),`./dev.sh harness berxel_mix_replay` 出 ✅。⑦ 双 ABI APK BUILD SUCCESSFUL,MIX 资产已打包进 app-debug.apk。**剩真机门控**:camera 回 2510DRK44C 后 `--es stream dual`(enableColor=true,默认)验 color_chunks>0 && depth_seq≥100;host 已证配方+并发实现正确,真机仅剩 Android USB stack/OTG 供电变量(带电 hub 兜底,见 [[finding_powered_hub_unblocks_vivo_dual_stream_2026-05-27]])。**根因订正**:之前"master color 流本身与 companion depth 在 2510DRK44C 互斥/设备特定"是错的——真因是我们 NATIVE_REWRITE **没进 MIX 模式**(发的是 SINGULAR master init,设备不协调双流);进 MIX(原厂 21 条序列)后 host 双流并发 0 错。详见 `docs/agent-memory/finding_p100r3_mix_color_depth_2026-06-02.md`。 **★★★ 2026-06-02 Android 真机 PASS(2510DRK44C 直插 OTG 无 hub)★★★**:`--es stream dual --ez master_rgb true`(enableColor=true → MIX)实测 **depth_seq 稳增 17→272、ir_skipped=1、pairs=271、valid=1.000、center_median≈2335mm(metric)、depth_chunks=3112 depth_err=0、color_chunks=7031 color_bytes=307MB color_err=0、q_drops=0 cq_drops=0**,color MJPEG#1400 解码 640×400→rgb24,无 setup 失败/无掉线/无 error-71。**color+depth 并发 RGBD 在 Android 真机彻底打通——一个月"开 color 整机死"的阻塞由 MIX 配方根除**。验收门(color_chunks>0 && depth_seq≥100)远超达成。MIX 模式 keepalive 关闭(ka_ok=0,master color bulk 自维持主控,不再需 set_cur 心跳)。depth marker 实证全 `0x0600`(demux 正确)。注:首次装后第一跑可能 depth_seq 卡 1(USB 冷态未settle/丢广播残留),洁净重装+单广播即稳——属测试时序,非代码问题。 **★ 2026-06-02 ④ 收尾完成:删冻结 legacy `berxelDual*`**:不退化门「conf/IR/16-long stats vs legacy 一致」**by construction 满足**——代码级证 (A)生产 `berxel_get_stats` 与 (B)legacy `berxelDualStats` 16 字段逐位同序同锁;(A) BerxelSessionAdapter 把 depth_mm/conf/IR/color/dump 全路由到同一 `berxel_snap_*`/`berxel_take_color`/`berxel_dump_depth` core(B 即从此 core 逐字拷贝),且 (A) 已 depth+color 真机 PASS。运行时 A-vs-B 比对=比对同一份 core,冗余。**删除**:native `berxel_dual_session_jni.cpp` Android-only `#ifdef` legacy JNI 段(1339→1037 行,8 个 `berxelDual*` JNIEXPORT)+ Kotlin `NativeBridge.kt` 8 个 `berxelDual*` external fun(60 行)+ 全部 stale 注释订正(camera_session_jni/adapter.h/BerxelService/BerxelNativeStack)。**验证**:`./dev.sh build`(native arm64+v7a + APK)✓、`./dev.sh test`(单测)✓、host probe 双目标编译 ✓,生产唯一路径 = cameraOpenByFds → BerxelDriver。(注:`./dev.sh ci` 有预存 lint 失败在 feature:message ChatMessageRow/VideoCallScreen,源自 commit f7951b9,与本 ④ 无关。)**M6.8b ④ 闭环**。**★ host 统一(2026-06-01,服务器真机已验证)**:`berxel_dual_session_jni.cpp` 经 `#ifdef __ANDROID__` 守(jni.h/android-log/8 JNIEXPORT),host(Linux 服务器 libusb 枚举)与 Android(wrap_sys_device fd)**双目标编译**;`berxel_open_dual` 拆 `set_modes_from_cfg`+`berxel_setup_and_launch`(公共尾)+ `_fd`/`_host` 两开法;`BerxelDriver::open_host` 接 `UsbContext` 枚举 0x0603+0x3558。**开发服务器真机跑 `tests/harness/berxel_camera_host/` 实测**:经统一 `ICameraDriver::open_host` 出 depth **valid=1.000、center≈345mm、5 帧全 100% 有效、analyze 判正常**(与 Android open_fd 同 setup 序列)。host probe 编译保护进 `scripts/eys3d-host-test.sh`。**★ 原厂 oracle parity harness(2026-06-01,已建 + 真机 PASS)**:`tests/harness/berxel_camera_parity/` —— 原厂 Linux SDK dense 采(640×400)+ 统一 `open_host` 采(1280×800)→ **分布级 mm 对齐**(median/p10/p90/有效率,分辨率无关:静态场景中位与分辨率无关,绕开逐像素网格不匹配)。`close_session` 加 host-only `libusb_attach_kernel_driver` 重附(`#ifndef __ANDROID__` 守,Android 字节不变),让原厂 SDK / kernel uvc 与我们 libusb 会话**共存不互斥**。**2026-06-01 真机 PASS**(物理重插 master 后跑通):原厂SDK `p10=234 p50=286 p90=581 有效率=99.8%` vs open_host `p10=233 p50=286 p90=583 有效率=100.0%`,**median 偏差=0.1% / p90 偏差=0.4%** → 统一厂商无关抽象层产出 = 原厂 SDK 产出,**最强不退化证据落地**。判定阈:median ≤5% + p90 ≤12%(实测远优于阈值)。一键复跑 `./dev.sh harness berxel_camera_parity`(原厂 1280 不出帧已改用实证 640 档)。⑤◑ **feature:scan3d 数码拓印路由(中性接口+加性改造,2026-06-01 主体落地)**:✅ deviceTypeLabel UI(`DepthCameraSubScreens`「型号」行,`CameraModel.fromUsbIds`)+ ✅ `CameraDetection`(枚举 USB→`CameraModel`)+ ✅ **`CameraSource`/`CameraSourceState` 中性接口**(两相机出同一 `ColorFrame`/`DepthFrame` 契约)+ ✅ **`Eys3dCameraService`**(独立流服务:USB 权限 receiver→单节点 `openDevice` fd→`CameraStack`→poll 循环 emit core:model 帧→引用计数生命周期,镜像 BerxelService,零厂商 SDK)+ ✅ **`BerxelService : CameraSource`**(纯加性:已有同名帧流/acquire/release 补 `override`,新增 `deviceLabel`+由 state lazy 派生 `sourceState`,流式行为零变)+ ✅ **`CameraSourceProvider`**(按 `CameraDetection` 选活动源,**默认回落 Berxel→不退化**)+ ✅ **`Scan3dRecordingViewModel`/`Scan3dRecordingScreen` 中性化路由**(录制/拓印主路改依赖 `CameraSource`,`deviceState:CameraSourceState`,`deviceShortText` 厂商无关)。`:core:native-bridge`+`:feature:scan3d` 编译过、**整包 APK + 全模块 Hilt 聚合 + debug/release 单测全过**。**暂留**(有意):DepthCameraViewModel/Scan3dViewModel/SonixDebug 仍 Berxel 专属工具,待 eYs3D 控件面 device-gated 后再中性化。**真机门控**:mode25 真值 + 单节点 USB 权限/带电 hub 双流出帧(`tests/harness/eys3d_mode25/`)。 | 两相机数码拓印都出 metric 深度;Berxel 无回归;真机 `cameraOpenByFds` 单设备双流 ≥100 帧无 null。 | `native/eys3d/{android,host}/`、`native/jni/camera_session_jni.cpp`、`core/native-bridge/.../camera/`、`feature/scan3d/` |

## M6.9 HLSD8 RGB 第二相机接入 + 双相机正射图（2026-06-10）

> **重大发现**：扫描机是**两颗物理独立 USB 相机**（`dumpsys usb` 实证）——深度 RS-D550(0x3438:0x0206) + **13MP RGB HLSD8(0x0C45:0x6366, Image+/Sonix, ~4160×832 MJPEG)**。gomob 此前只接深度，HLSD8 从未触及（它是 VINCreator 正射图的真彩源）。
> docs: `docs/agent-memory/finding_hlsd8_rgb_second_camera_2026-06-10.md`、`docs/architecture/08-vin-rectify-design.md`

| 子项 | 内容 | 验收 | 文档/路径 |
|------|------|------|-----------|
| ~~M6.9.1~~ | **✅ HLSD8 native 取流(color-only)**：`native/hlsd8/hlsd8_uvc_session.{h,cpp}` —— `Hlsd8Driver:ICameraDriver`(认领 0x0C45:0x6366,注册进 `camera_session_jni.cpp Registry()`)+ `Hlsd8UvcSession`(dlopen `libuvc_lusb100.so` 同 eYs3D 后端,`uvc_get_format_descs` 枚举设备真实格式**自动选最大 MJPEG 帧**,`uvc_start_streaming`,`snapshot_color` consume-once)。无 XU/无手动 bulk(标准 Sonix UVC)。 | ✅ 双 ABI `.so` 编译过(`-Wpedantic`)。**真机出流 device-gated**(用户回来测)。 | `native/hlsd8/`、`native/jni/camera_session_jni.cpp` |
| ~~M6.9.2~~ | **✅ 双相机 Kotlin 架构**：`CameraModel.Hlsd8`(`isDepthSource=false`)+ `CameraDetection.detect`(只选深度源)/`detectAuxRgb`(HLSD8)分流——两颗并存;`Hlsd8CameraService:CameraSource`(color-only,无 armViaJava,13MP 预览 `inSampleSize` 降采样);`CameraSourceProvider.auxRgb()`;`usb_device_filter.xml` 加 3141:25446。 | ✅ `./dev.sh build` 全链编译+Hilt 过。 | `core/native-bridge/.../camera/` |
| ~~M6.9.3~~ | **✅ UI 深度页加 HLSD8 RGB 预览**：`DepthCameraViewModel` 并行 acquire/release rgbSource + `_rgbPreview`/`rgbStat`;`DepthCameraScreen` 插着 HLSD8 时多「RGB · HLSD8」栏,eYs3D 那栏标签改「L'(左矫正)」。 | ✅ 编译过。真机视觉验证 device-gated。 | `feature/scan3d/DepthCamera*` |
| ~~M6.9.4~~ | **✅ 双相机正射图几何**(替代旧 `vin_rectify.cpp` NOT_IMPLEMENTED 桩)：`native/vin/ortho_rectify.{h,cpp}` —— depth 反投影→RANSAC 主平面→平面内正交基→逐正射像素→`R*Q+t` 变 RGB 系→投影双线性采样。harness `tests/native_host/ortho_rectify_test.cpp` PASS(法向|n·n0|=1.0/inlier1.0/rms0.29mm/覆盖91%/中心-质心0.55mm/尺度比1.008),挂 `scripts/native-host-test.sh`。 | ✅ host harness 全绿。 | `native/vin/ortho_rectify.*`、`tests/native_host/ortho_rectify_test.cpp` |
| ~~M6.9.5a~~ | **✅ 正射拓印端侧竖切（2026-06-18，code-complete 待真机）**：JNI `vinOrthoRectify` 实接 `OrthoRectify`（删 `vinRectify` NOT_IMPLEMENTED 桩 + 删死桩 `vin_rectify.cpp`，回裸 RGB+mask 不编 PNG）；`NativeBridge.vinOrthoRectify` 双相机契约 + `VinOrthoNative`；`VinCaptureViewModel.capture()` 配对 depth+eYs3D L' → 正射 → `FrameRenderer.orthoToBitmap` → `RubbingPaper` 显示；快门/重拍接 VM。**第一光用 eYs3D L' + R\|t=单位阵**（L' 与深度同一矫正左目，几何成立非假兜底）。OCR 留 `recognize()` 待接。 | build 绿 + `ortho_rectify_test` PASS（已达）；真机：对平面 VIN 钢牌点快门，拓印纸面出正射还原图（待设备回线验证）。 | `native/jni/jni_bridge.cpp`、`NativeBridge.kt`、`VinCaptureViewModel.kt`、`ScanCaptureScreen.kt`、`docs/architecture/08-vin-rectify-design.md §9.1` |
| ~~M6.9.5b~~ | **被 M6.9.10 收编**：原计划「HLSD8 高清正射 = 服务端双相机标定」。2026-06-18 离线实测证彩色=深度精确 2× **registered（零视差），重合零标定即成**；HLSD8↔depth 标定仅为提清晰度，非重合必需。ArUco 服务端标定保留为后续可选增强（`device_calibrations` migration 0009 + 激光管线 solvePnP 复用仍有效）。 | — | `docs/architecture/08 §9.2/§10` |
| M6.9.10 | **★VIN 还原全量上服务端 + 原厂全保真（架构决策 2026-06-18，用户拍板）**：还原算法整条移到服务端 Go cvengine(gocv)，端侧只「拍+存+传」。**已完成**：① 端侧 `VinCaptureViewModel.capture()` 落原始采集 `vin_captures/cap_*`(rgb1300.jpg+depth.yuv+meta.json)，真机采 11 张/2 板；② 原厂 `libcreator_jni.so` 还原管线**全量逆向+对抗校验**（3 工作流）落 `docs/architecture/08 §10`；③ 离线 harness `tests/harness/vin_restore/`（深度 RANSAC 平面→tilt>70门→OBB 单应正射→原厂 GetSignature3G 去阴影 adaptiveThreshold 131/15）**结论=正常**：同 VIN 各张几何残余中位 0.48%、叠加成锐利可读 VIN、板2 反光根除。④ **✅ Go cvengine 端口**：`server/internal/cvengine/restore/`(obb/plane/render/signature/restore.go) + `POST /cv/ocr/v1/vin_restore` 端点 + `gocv.RunCom`(CreateORTCom 通用张量桥) + core `KindCom`。OBB 走 CreateORTCom 原始 [1,6,8400] Go 解码；平面 z=ax+by+d 用 `gocv.Solve`(SVD-free)；单应 8×8 `Solve`(GetPerspectiveTransform 仅整型)；去阴影 adaptiveThreshold 131/15。**验收过**：`go build ./cmd/cvengine` 绿；11 张真机自验 Go vs Python **7/11 逐像素一致、最大差 0.106%**(RNG 种子差,内容同)；Go 出图跑同一 analyze=正常(中位残余 0.48%)。运行期需 `LD_LIBRARY_PATH=/usr/local/lib:/usr/local/lib64:/usr/local/onnxruntime/lib`(opencv_world.so.405 在 lib64)。⑤ **✅ app 上传→服务端还原→回显**：`CVEngineApi.vinRestore`(multipart image_binary_rgb1300+image_binary_depth+depth_w/h+fx/fy/cx/cy+device_id,走 cv/ocr/v1/vin_restore devserver 前缀反代) + `VinRestoreResponse` DTO + `VinRepository.restore()`(base64→PNG 字节,repo 内解码) + `VinCaptureViewModel.capture()`(压一次复用字节→落盘→端侧即时占位预览→**上传服务端权威签名图覆盖 `_rubbing`**+倾角/内点/检出提示;tilt 判废走 200/ok:false 友好提示,上传失败走"已存可重试"不丢盘)。三模块 `compileDebugKotlin`(含 KSP)绿,字段逐一对齐服务端 handler；**服务端 HTTP 契约 httptest `TestVinRestoreHTTPContract` 用真机 11 张数据直打 `h.VinRestore`：11/11 返 code==0+ok=true 合法 PNG(1200×260,tilt 1.9–7.0°,检出 1)**,证 multipart 线格式整条服务端缝隙通。**待**：⑥ OCR 接 `vin_pipeline`(还原签名→识别)；⑦ 模型部署(yolo-obb.onnx 入 model-registry,现 `handler.go` 默认 `.dev/vin_models` 靠 env `VIN_OBB_MODEL` 覆盖)；⑧ 端侧 Android live 回线(服务端 HTTP 契约已真机数据验,剩 OkHttp 实发+回显观感,device-gated)。 | harness `run.sh` 退出 0=正常 ✅；`go build ./cmd/cvengine` 绿 ✅；Go 单帧自验与 Python 一致 ✅；⑤ 端侧三模块编译绿 + 服务端 HTTP 契约 httptest 11/11 真机数据出 PNG ✅。 | `tests/harness/vin_restore/`、`docs/architecture/08 §10`、`server/internal/cvengine/restore/`、`server/internal/cvengine/handler.go` |
| ~~M6.9.6~~ | **✅ mode25 软件链与 VINCreator 逐字对齐(2026-06-11 续31)**：① arming 是 **counter 门控握手**(selector 0x0a 滚动 nonce),旧静态回放 164 写失败 → 改 **live-counter 全回放 2049 条**(`Eys3dCleanArming.kt` 从 `.dev/vinshim/rsd550_clean_seq.txt` 生成),956 flash 全成功;② `eys3d_pupil_session.cpp` libuvc 流 **IF2 深度 YUYV 640×128**(IF1 只 PROBE 不 COMMIT,同 VINCreator);③ **增强 shim 抓 VINCreator bulk 实证**:`ep=82 len=1024 timeout=5000` 与 gomob 一致,稳态 20s 0 控制活动=**无 keepalive**;④ libuvc 加 `flags=2`=跳 SET_INTERFACE(VINCreator 全程无)。 | ✅ 控制序列+bulk 参数与 VINCreator 逐字相同。 | `finding_eys3d_android_bringup_0bytes_2026-06-09.md` 续31 |
| M6.9.7 | **mode25 真深度帧验证(device-gated,需用户重插)**：当前 depth bulk 全 `status=2 TIMED_OUT`、0 帧,但**干净 VINCreator 此刻也一样**(红框/无 SN/全超时)→ **设备被反复冷启/claim 循环 wedge**(本会话首跑 VINCreator 曾 SN:476 能产),软件无法恢复(reset 更 wedge)。**用户操作**:物理重新插拔深度相机 + 对准 20+cm 有纹理场景 → 进 gomob 深度页看 `eys3d_pupil depth#N [丰富?真深度]`/`tick depth>0`。**验证后清理**:`stream.c` 的 `uvc_stream_diag` 临时日志 + `Eys3dProvenArming.kt`(已弃用)。 | 重插+有效场景后 gomob depth ≥100 帧、metric mm 合理。 | `finding_eys3d_android_bringup_0bytes_2026-06-09.md` 续31 |
| ~~M6.9.8~~ | **✅→被 M6.9.9 取代：真立体深度突破（2026-06-15）**：确立 mode25 真深度根因 —— eYs3D 彩色须 **mode25 原生 1280×256 MJPEG**（传感器 binned）；2560×960 全分辨率 → 深度 ASIC 吐固定罐头帧。配套：color 暖机、HLSD8 补光灯、IR 彩色后设（e0=3/e2=6）、AE=8、keep-surface。Java setFrameCallback 路稀疏 7~23%；**已被 native 直驱厂商 C++ 路（M6.9.9）取代，vendor DepthFilter 补洞后 valid 升至 50~78%**，Java `Eys3dApcCamera.kt` 退役。 | ✅ 根因确立并落地到 vendor C++ 路。densify 进一步优化随主线推进。 | 历史：续45/46/47 → 现 M6.9.9 |
| ~~M6.9.9~~ | **✅ native 直驱厂商 C++ 引擎（零 Java 编排）+ spike 收口（2026-06-17，build 绿）**：绕开自研 -EPROTO 硬墙，改 dlopen libUVCCamera.so 直调 `UVCCamera/UVCPreview/FrameGrabber` C++ 类起 mode25 流，FrameGrabber 回调 repoint 到 native trampoline（纯 C 指针，零 JNI 接帧），仅留 `UsbManager.openDevice` 拿 fd + `bindEys3dVendorJni` 的 setVM。真机 2510DRK44C 实测 L'+深度热力图+HLSD8 RGB 全渲染、~4-7fps 不崩。**收口四项**：① 真内参 `rsd550Intrinsics`（出厂矫正基准全幅 1280×960 各向异性缩放）替 zeroIntrinsics；② spike 清理（poll 日志降级、external-storage 经 options_json 下发 app 专属目录、kUseVendorCpp 正名）；③ 退役 Java `Eys3dApcCamera.kt`（去 `USE_INDEPENDENT_NATIVE` toggle，加 proguard `-keep com.esp...`，vendor shim 类留作 setVM 绑定）；④ harness `tests/harness/eys3d_vendor_cpp/`。 | ✅ build 绿；起流链/fps/valid/零JNI harness 可判定。**尾巴收口**：v7a 闭环（加 64 位守门，无现役 32 位设备，补 v7a 需匹配 vendor 二进制+重做 32 位 RE）；fy 缩放-vs-裁剪为 device-gated（验收=已知尺寸量测，见 §内参），将被 M6.5 flash 内参取代；per-device flash 内参 = M6.5 里程碑。 | `eys3d_vendor_cpp_session.*`、`vendor_uvc_abi.h`、`Eys3dCameraService.kt`、`docs/architecture/13-eys3d-driver.md §3bis`、finding_eys3d_zero_vendor_independence |
| ~~M6.9.8-streaming~~ | **✅ 全 Java ApcCamera 路径取 mode25 深度【流】通 + 生产清理（2026-06-15）**：2510DRK44C 直插 OTG 实测 `depth#1..840` 持续稳定 ~2fps、UI「DEPTH·TURBO·2fps·640×128」渲染。两决定性修正：①彩色须 **2560×960 YUYV**（非 CameraModeKt 预设 1280×256 MJPEG，后者双端 status=2 全超时）；②深度视差取 **低 11 位**（`raw & 0x07FF`，高 5 位是 flag；不掩码越界 ZD[2048]）；③彩色回调 **drain-only**（native 线程做 2.4M 像素转换会 bulk 背压致 stall）。生产清理完成：libusb100 shim→VINCreator 原版（两 ABI md5 2508/f3706，APK 已验）、删 native-direct 死码（Eys3dApcSession/cameraOpenEys3dApc/nativeApcFrame/Eys3dApcFrameSink/CameraStack.startEys3dApc/Eys3dProven+CleanArming.kt）、诊断日志降为健康日志。HLSD8 并发 RGB 暂禁（裸 OTG 供电/带宽，待带电 hub，M6.9.5）。 | ✅ 真机 depth ≥180 帧、metric mm 合理（459mm）、内容非退化噪声、生产无 shim/无死码、build 绿。 | ~~历史~~（已完成）：`Eys3dApcCamera.kt`、`Eys3dCameraService.kt`、`native/jni/camera_session_jni.cpp`、续42/43/44 |

## M2 iHawk 标定

> 触发条件：M1.3 的 SDK 出厂参数或 `setRegistrationEnable` 实测不达标。
> docs: `docs/architecture/05-calibration-pipeline.md`

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M2.1 | calibration native 模块接 OpenCV 4.x，使用 `cv::aruco` / `cv::calibrateCamera` / `cv::stereoCalibrate`；同时决定复用 SDK `libopencv_java3.so` 还是自编 OpenCV 4。 | native host build 通过；Charuco 单图检测能输出角点、id、reprojection 输入数据。 | `docs/architecture/05-calibration-pipeline.md` §3.3 |
| M2.2 | 新建 `feature:calibration`，实现 Charuco 检测向导 UI，按 12 个角度采集 Color/Depth pair。 | 真机采集 12 组样本落 `.dev/calibration/<sessionId>/`；uiautomator / instrumentation 覆盖采集页与结果页关键状态。 | `docs/architecture/05-calibration-pipeline.md` §3.2 |
| M2.3 | 求解单目内参与 Color/Depth 外参，输出可序列化标定结果。 | Color reprojection ≤ 1.0 px；Depth reprojection ≤ 0.5 mm；失败样本能给出可解释原因。 | `docs/architecture/05-calibration-pipeline.md` |
| M2.4 | 本地 Room 保存 `calibrations`，以 `deviceSerial` 唯一索引复用；接服务端 device calibration 同步。 | 扫描启动前能比对本地 sha256 与服务端 latest；一致跳过，不一致拉取并更新本地。 | `core:database` / `docs/architecture/05-calibration-pipeline.md` |
| M2.5 | 建 `calibration_quality` harness，对比 SDK 出厂参数与自标定结果。 | `./dev.sh harness calibration_quality` 输出“正常 / 警告 / 异常 + 原因”。 | `tests/harness/calibration_quality/` |

## M3 多视角 RGBD 重建

> 当前权威路线：多视角 RGBD 配准 + 端云融合。
> docs: `docs/architecture/04b-multiview-rgbd-reconstruction.md`
>
> 历史 native ICP / scan session / Filament 预览沉淀只作为阶段 3 复用背景；旧 TSDF 实时主线不再扩展。

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M3.12 | 端侧采集模式 UI：引导用户环绕采 8-12 角度；每次拍照保存 RGB、depth、内参、时间戳；显示上一张 RGB 半透明叠加；端侧 ORB overlap < 30% 时提示重拍；中心连通块 ROI 作为初步分割兜底。 | `.dev/scans/<sessionId>/` 内至少 8 组 RGBD pair 完整；每组 RGB/depth timestamp 差 ≤ 50ms；uiautomator / logcat 确认采集引导、上一张叠加状态、低 overlap 重拍提示可达。 | `docs/architecture/04b-multiview-rgbd-reconstruction.md` §3.1 |
| M3.13 | 接 asset multipart upload：复用 M-S2.3 分片上传；保存 multiview session 元数据，可放 `inspections.scan_multiview_payload` 或独立 scan 表。 | curl 上传 8 组 RGBD pair 到 MinIO 成功；DB 记录 sessionId、frameCount、每帧 sha256。 | `docs/architecture/04b-multiview-rgbd-reconstruction.md` §3.2 / `docs/architecture/server/02-api-contract.md` |
| M3.14 | cv-engine 加 `object_3d_fusion` worker：NATS 订阅 `scan.captured`，拉帧后跑 Open3D multiway registration、Color-ICP、全局 PGO、Marching Cubes、纹理烘焙，输出 GLB。**承接 M1.6.20 端侧 conf 加权语义到云端**。**首增量(2026-05-31,算法核+harness 先行)已落**:`server/fusion_service/fusion_core.py` —— per-view 点云 → **全连接 multiway**(注册所有 i<j 对,相邻=里程计确定边、非相邻=闭环不确定边,line process 剪坏边)→ 全局 PGO → conf **阈值预掩码**加权(Open3D 无 per-point 权重的可行等价,配准+积分共用同一 conf-masked 深度)→ `ScalableTSDFVolume` → MC;`o3d.utility.random.seed(0)` 定随机,`fuse()` 重锚 cam0 保 world==cam0。**踩坑订正**:纯顺序位姿图脆弱(一处误配翻 141°/741mm 致下游崩坏)→ 改全连接;补底面覆盖加仰角振荡反裂顶/底两簇致底簇翻转 → 回退单环轨道 + 改"观测面"chamfer。**第二增量(2026-05-31,完整 Go worker 垂直切片)已落**:照 asr_service/asrworker 先例落 `fusion_service` HTTP `/fuse`(收 RgbdShot bundle zip→融合→trimesh 导 GLB,Open3D 0.19 自带 .glb 写出损坏)+ `0016_scan_fusion` DB 队列 + `pkg/repo/scan_fusion.go` + `internal/fusion/{client,worker}` + `cmd/fusionworker`(轮询领→MinIO 拉→/fuse→GLB 存 MinIO→发 `scan.fusion_done`)。**第三增量(2026-05-31,UV-atlas 纹理烘焙)已落**:`bake_albedo`(`compute_uvatlas` iso-charts + `project_images_to_albedo` 多视角回投影,可见性/混合由 Open3D 用 mesh 几何处理)+ `textured_mesh_to_glb`(三角 UV 去索引为逐顶点 UV + v 翻转,trimesh 导带纹理 GLB);`/fuse` 加 `texture` 参数,worker 默认开(`GOMOB_FUSION_TEXTURE`)。**待后续增量**:真实 8 张 P100R3 RGBD 端到端(卡物理多视角采集)、端侧 upload UI(M3.12)、端侧 gallery 订阅 `scan.fusion_done`(M3.15)。 | **算法核 harness `scan_fusion` 两门过(2026-05-31,Open3D 0.19)**:① 干净 chamfer **1.25–1.37mm ≤ 5mm**(三跑;GT 位姿对照 d_fg=1.3mm 证配准逼近真值);② 带噪 40% 弱回波+飞点 conf 加权 **~1.6mm vs 不加权 ~35mm,降 ~96%**。**纹理 harness `scan_fusion_texture` 过**:低多边形(voxel20→~300 顶点)+ 正弦纹理,纹理色误差 **0.040 vs 顶点色 0.084,降 ~52%**(图像分辨率 vs 稀疏顶点)。**全链路 harness `scan_fusion_e2e` 端到端过(含带纹理路径)**:真实上传(init/part/complete,kind=scan3d_bundle)→自动入队→worker→/fuse→带纹理 GLB(3594 顶点/643KB)存 MinIO→DB done + `scan.fusion_done` 事件 + 端到端 GLB chamfer **1.37mm ≤ 5mm**;`go build/vet` 净、repo 测无回归。〔待〕真实 8 张 RGBD 端到端 1-2 分钟 ≥ 50K 顶点 mesh。 | `server/fusion_service/{fusion_core,app,rgbd_bundle}.py` / `server/migrations/0016_scan_fusion.*` / `server/pkg/repo/scan_fusion.go` / `server/internal/{fusion/*,asset/handler.go}` / `server/cmd/fusionworker/main.go` / `tests/harness/scan_fusion{,_e2e,_texture}/` / `docs/architecture/04b-...md` §3.3/§5.5 |
| M3.15 | 端侧 gallery 拉取 GLB 回看：订阅或轮询 `scan.fusion_done`，下载 GLB，用 Filament PBR + IBL 渲染，并支持旋转、缩放、平移。**服务端实时推送桥已落(2026-05-31)**:融合任务加 `owner_user_id`(上传发起者,迁移 `0017`,经 asset complete 入队透传)→ `FusionDoneEvent` 带 owner → `signaling.FusionBridge` 订阅 NATS `scan.fusion_done` 按 owner_user_id `hub.Push` 给该用户在线 ws 连接(帧 type=`scan.fusion_done`,payload 原样转发);`cmd/signaling`+`cmd/devserver` 经 `GOMOB_NATS_URL` 守卫接入(NATS 不可用只降级关推送,不拖垮消息/通话)。**待**:端侧 Android/Filament gallery 订阅该 ws 帧 + 下载渲染 + 手势(需真机视觉验证,emulator 跑不了媒体)。 | 服务端:`scan_fusion bridge` 单测 4 例过(owner 命中/无 owner/owner 离线/坏 JSON)+ 真 NATS 往返测过(`scan_fusion_e2e` 5c:发事件→owner ws 收到);全链路 harness `scan_fusion_e2e` 仍过(5a owner 透传 owner=14、chamfer 1.34mm)。〔待〕1080p 真机回看流畅,Filament/instrumentation 确认模型非空、首帧成功,旋转/缩放/平移手势可用。 | `server/internal/signaling/fusion_bridge.go` / `server/migrations/0017_scan_fusion_owner.*` / `server/pkg/repo/scan_fusion.go` / `server/internal/{asset/handler.go,fusion/worker.go}` / `server/cmd/{signaling,devserver}/main.go` / `docs/architecture/04b-...md` §3.4 |
| M3.16 | 建 `scan_multiview_quality` harness:合成 Stanford Bunny 8 角度 RGBD + 真实卡车数据,跑端到端质量评估。**已落(2026-05-31)**:vendored Bunny(Open3D 抽稀 ~12k 面 / 302KB / 离线)→ 复用 `scan_fusion/synth_dataset` 渲染+观测面 → `fusion_core` 端到端重建 → `quality_bench.py` 量 chamfer/覆盖度/UV 写 metrics.json,`analyze.py` 判定(`./dev.sh harness` 自动调)。真实卡车数据:`GOMOB_TRUCK_DATASET` 指 RgbdShot bundle 则跑(无 GT 仅出统计),未就绪诚实 skip(非桩,卡 M3.14②)。**★harness 暴露并修复 M3.14 配准脆弱性**:旧固定 FPFH 12mm/对应 30mm 对复杂有机体特征过粗、宽基线对误配翻转(chamfer 3/7.5mm 乱跳);改 `reg_voxel=min(voxel,12mm)`、对应=2.5×reg_voxel(`fusion_core.reg_voxel_m/reg_corr_m`)→ Bunny 8 视角 7.5→1.76mm 稳定。**UV≥70% 实测不可达订正**:iso-charts 与 xatlas 在 MC 有机网格都只 ~62–70%(几何属性非工具),改 UV 为**软报告**,硬门取 chamfer+覆盖度(扫描真实化本质;经用户确认)。 | `./dev.sh harness scan_multiview_quality` **通过(2x 稳定)**:chamfer **1.76mm ≤5mm**、coverage@5mm **96%≥78%**、@10mm **99.8%≥92%**;UV 利用率 ~62–70% 软报告。回归:fusion_bench ①1.31mm ②−96%、texture −51% 均不退。〔待〕真实卡车 RGBD(卡 M3.14②物理采集)。 | `tests/harness/scan_multiview_quality/{bunny_dataset,quality_bench,analyze}.py` / `assets/bunny.ply` / `server/fusion_service/fusion_core.py`(reg 尺度派生) / `docs/architecture/04b-...md` §5.1 |
| M3.17 | cv-engine 接 SAM-HD 或 SAM2 服务端分割：用户 prompt + RGB 生成 2D mask，再借 iHawk 像素对齐投到 depth。**第一增量(2026-05-31)已落 —— 服务端 HQ-SAM**：新建 `server/sam_service`(Python FastAPI + GPU,对标 fusion_service)。选 **HQ-SAM `vit_h`(sam-hq,Apache-2.0 可商用)** —— 高质量 token,边界比原版 SAM 锐。`sam_core.segment(image,box/points)` + `/segment` HTTP(收图+框/点→mask PNG)+ `/healthz`。**当前边界:框由人工给**(不做自动 grounding;NVIDIA LocateAnything 等开放词表 grounding 非商用许可,暂不引入)。box 提示定尺度→单 mask,避开点提示粒度歧义(点轮胎=轮胎/整车)。torch CUDA + 2.5GB 权重经 aria2c 离线下(权重不进 git);run.sh 自动挑空闲 GPU 防 OOM。**第二增量(2026-05-31)已落 —— mask 投 depth 接进融合主线**:RGB 与 depth 端侧已对齐(rgbd_bundle 契约)→ mask 逐像素直接对应 depth,无需跨传感器外参;`RgbdFrame.mask` 作与 conf 平行的**深度预掩码通道**(`_masked_depth`,mask 外像素不入点云/配准/积分),bundle 加 `mask_{i}.u8` 通道(向后兼容),masks 随 bundle 走、fusion 不回调 sam_service(服务解耦)。加 `mask_erode_px` 旋钮(默认 0,真机边界飞点用)。**★harness `scan_mask_fusion` 暴露并处理 2 点**:①目标用周期正弦纹理致 FPFH/Color-ICP 特征混叠→~20% 位姿翻转(换非周期渐变色 10/10 稳),记 finding;②erode 在合成 clean 边界单调削覆盖无收益→默认 0。**第三增量(2026-05-31)已落 —— 与 M3.14 阶段1 启发式 ROI 的 A/B**:忠实复刻 native `BuildForegroundDepth`(中心 ROI P25 种子→动态深度带→4-连通中心加权块)作 `heuristic_roi.py`,与 SAM 同人工框对比(steelman)。验证 04b §5.2 阶段2 验收"mesh 边缘毛刺 vs 阶段1 减少 ≥80%"。启发式纯深度法切不掉贴地目标基座的地面裙边;SAM 按外观切净。**A/B 与 erode_sweep 均改固定位姿重积分**(三 mask 共用 GT 干净位姿,隔离 Open3D RANSAC 非确定翻转 → 纯比分割,门稳定不被伪因刷过;固定位姿后启发box 仍 ~35% 毛刺=真裙边非翻转)。新增门⑦。**待后续增量**:端侧 MobileSAM/SAM2 轻量化、真实图像基准。 | harness `sam_segmentation` 过:9 尖星形 + 松框 → **IoU 0.9916 ≥ 0.92**;HTTP smoke 过。harness `scan_mask_fusion` 过(端到端 SAM→mask→融合,多次稳定,7 硬门):SAM IoU **0.994**、mask 引导 chamfer **~1.7mm**/污染 **0.01%**/cov@5mm **94%**、baseline 污染 **~87–97%**、对照砍 **~9000×**、与 GT-mask 上界同量级;**A/B 边缘毛刺降 ~93–95% ≥80%(固定位姿受控)**(SAM 毛刺 ~2% vs 启发式 box ~35%、顶点约 SAM 1.7×)。回归:scan_fusion ①1.33mm ②−96%、scan_multiview_quality 1.76mm/cov96.5% 不退;bundle mask 往返+`_masked_depth` 语义校验过。〔待〕真实图像基准、端侧轻量化。 | `server/sam_service/{sam_core,app}.py` / `server/fusion_service/{fusion_core,rgbd_bundle}.py`(mask 通道+腐蚀) / `tests/harness/scan_mask_fusion/{clutter_dataset,heuristic_roi,mask_fusion_bench,analyze}.py` / `docs/architecture/04b-...md` §5.2 |
| M3.18 | GPU worker 部署：cv-engine 容器接 GPU runtime，支持模型卸载与任务排队。 | 单 worker 处理 8 张 RGBD 的 SAM 推理 ≤ 30s；队列满载时状态可观测。 | `docs/architecture/04b-multiview-rgbd-reconstruction.md` §4 |
| M3.19 | 端侧 SAM2 Mobile：模型进 APK，适配 NPU/GPU 后端，首帧 prompt 后做 mask propagation。 | LOG-AN10 上 SAM 推理稳定 ≥ 5 fps；失败时给出设备能力原因。 | `docs/architecture/04b-multiview-rgbd-reconstruction.md` §5 |
| M3.20 | 端侧实时 ICP/TSDF + Filament mesh 预览：只融合 SAM mask 内 voxel，HUD 显示覆盖度与缺漏方向，云端高精度版完成后替换。 | 端侧实时 mesh 增长 ≥ 5 fps；扫描完成 ≤ 1s 出实时版；云端版完成后自动替换。 | `docs/architecture/04b-multiview-rgbd-reconstruction.md` §5 |

## M4 VIN 数码拓印

> 单帧 RGBD → ROI 平面拟合 → 固定法向距离正射重投影 → 1024×512 拓印图 → cv-engine OCR。
> docs: `docs/architecture/08-vin-rectify-design.md`

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M4.1 | vin native 模块：RANSAC 平面拟合、正射相机参数化、像素重投影、双线性 Color 采样、PNG 编码。 | native host test 覆盖平面拟合、ROI 越界、空深度、PNG 输出尺寸 1024×512。 | `docs/architecture/08-vin-rectify-design.md` §2 |
| M4.2 | `feature:scan3d` 加“VIN 数码拓印”入口：Color 实时预览、ROI 选框拖拽、拍照、拓印结果页。 | `./dev.sh run` 后通过 uiautomator / instrumentation 确认 ROI 拖拽命中区可操作、拍照链路可触发、拓印结果页可达。 | `feature/scan3d/Scan3dScreen.kt` |
| M4.3 | 拓印图本地落盘并推服务端 cv-engine `vin_pipeline`。 | 上传后能拿到 verdict、reasons、字符结果；网络失败保留本地重试记录。 | `core:network` / `docs/architecture/server/03-cvengine-migration.md` |
| M4.4 | 建 `vin_rectify_quality` harness：录制真实 VIN 钢架 RGBD pair，跑 `vinRectify` 与服务端 OCR。 | `./dev.sh harness vin_rectify_quality` 通过；多角度拓印 SSIM ≥ 0.9，OCR 准确率 ≥ 95%。 | `tests/harness/vin_rectify_quality/` |

## 服务端与治理待办

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| S1 | App 端接 device calibration 同步：扫描启动前拉 `GET /v1/devices/{id}/calibrations/latest`，与本地 Room sha256 比对。 | sha256 一致时不下载；不一致时拉完整 params 并更新；离线时使用本地最近可用版本并标明版本。 | `docs/architecture/05-calibration-pipeline.md` |
| S2 | shape compare 从元数据级升级到几何级：解析真实 mesh，补 chamfer / Hausdorff / scale consistency。 | 扩展 `cv_shape_compare` harness；真实 mesh 对比能输出几何指标与三态 verdict。 | `docs/architecture/06-product-features.md` §3.4 |
| S3 | 生成并接入 cv-engine gRPC server。 | 安装 protoc 后跑 `server/scripts/proto-gen.sh`；gRPC 端点与 `proto/cvengine.proto` 契约一致，保留 HTTP harness。 | `server/proto/cvengine.proto` |
| S4 | 更新 `docs/architecture/registry/` 机器真理源。 | 任何模块边界、依赖或能力成熟度变化后，同步更新 `modules.yaml`、`dependencies.yaml`、`capabilities.yaml` 并通过校验。 | `docs/architecture/registry/` |

## M7 端侧两大功能拉通（车辆外廓扫描 + VIN 数码拓印）

> 背景：底座全真（`CameraSource.active()` → 真 16bit metric DepthFrame + confidence，2510DRK44C 验过）、服务端全真（`scan3d_bundle`→融合 worker→`/fuse`→GLB→`scan.fusion_done` WS；cvengine `vin_pipeline` verdict）。缺口集中在端侧中段：入口卡片指向 mock 屏，真管线 `scan3d/recording` 是孤儿路由，Kotlin 缺 bundle 上传 + VIN 客户端。
> 决策（2026-06-02）：① 外廓重写成真 8 角度采集（主线 04b 多视角云端融合）；② VIN 先通业务链路（端采单帧 RGBD→服务端 `vin_pipeline` 真 verdict），native 拓印图第二刀；③ 验收=软件全通 + 2510DRK44C 可跑设备 + GLB 回看闭环，全机型真机出帧矩阵作独立门控。
> docs: `docs/architecture/04b-multiview-rgbd-reconstruction.md` §3 / `docs/architecture/08-vin-rectify-design.md` / `docs/architecture/server/02-api-contract.md` §5/§14
>
> **进度（2026-06-02）软件链路全通 + 编译/单测/harness 全绿**：M7.1-M7.6 已落码；两 mock 屏（VehicleContourScanScreen / ScanCaptureScreen）已重写为 VM 驱动接真底座，删尽硬编码 demo。双 ABI APK BUILD SUCCESSFUL + `./dev.sh test` + server `go test ./internal/asset` 全过；新建 harness `scan_bundle_roundtrip` 验端侧 Kotlin bundle 字节布局 ↔ 服务端 `rgbd_bundle.unpack`+`fuse` 跨语言契约（✅正常）；5 维度对抗 review（19 confirmed）已修：bundle 帧不完整 fail-fast、GLB ModelViewer 资源释放、`/v1/scans` owner nil 鉴权、EnvelopeErrorInterceptor 二进制不缓冲、capture/retake 协程竞态、GLB 下载截断清理。**详见 [[finding_scan_vin_wiring_2026-06-02]]**。**剩 M7.7 真机门控（受 M6.8b/M1.6 device-gating）+ RGB↔depth 真配准（approx resize 待 M2）+ VIN catalog 车型选择客户端**。

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M7.1 | bundle 上传核心层：`AssetUploadCompleteRequest` 加 `scan_session_id`/`frame_count`（向后兼容可空）；`core:data` 新增 `Scan3dBundleUploader` 把 N 张已对齐 RGBD（resize color→depth 分辨率 + depth 内参，approx 对齐文档化指向 M2 registration）打成 `fusion_service/rgbd_bundle.py` 契约 zip（manifest.json + rgb_i.png + depth_i.u16 + conf_i.u8），kind=`scan3d_bundle` 分块上传。 | host/单元：bundle zip 能被 `fusion_service.unpack` 解出 ≥2 帧；`./dev.sh test` 序列化字段；编译过。 | `core/network/.../dto/AssetDto.kt`、`core/data/.../scan/Scan3dBundleUploader.kt`、`server/fusion_service/rgbd_bundle.py` |
| M7.2 | fusion_done 实时事件：`core:realtime` 加 `RealtimeEvent.ScanFusionDone(sessionKey,resultObjectKey,vertices,triangles,frameCount)` + parser `scan.fusion_done` 分支。 | 单测：解 `scan.fusion_done` 帧出事件；坏 JSON 不崩。 | `core/realtime/.../RealtimeEnvelopeParser.kt` |
| M7.3 | `VehicleContourScanViewModel`：8 角度引导采集驱动 `CameraSource`，每角抓配对 color+depth → `RgbdShot`（点云预览 = 当帧深度反投影，真数据）；完成 → 打 bundle → `Scan3dBundleUploader` 上传 → 等 `ScanFusionDone(sessionKey 匹配)` → presign 下载 GLB → Completed。 | 真机/可跑设备：8 角度采真帧、bundle 上传入队、收 fusion_done、下到 GLB；logcat 全链路无 mock。 | `feature/scan3d/.../VehicleContourScanViewModel.kt` |
| M7.4 | 重写 `VehicleContourScanScreen` 为 VM 驱动：保留 8 角度环选交互，删硬编码 `shots`/假 canvas，接真预览（live color/depth + 当角点云）；新增 `GlbModelView`（filament gltfio）回看融合 GLB。`gltfio-android` 进 `feature:scan3d`。 | uiautomator/instrumentation：点卡片进真屏、环选可点、拍照触发采集、完成出 GLB 视图；无硬编码 demo。 | `feature/scan3d/.../VehicleContourScanScreen.kt`、`GlbModelView.kt`、`feature/scan3d/build.gradle.kts` |
| M7.5 | VIN 业务链路：`core:network` 加 `CVEngineApi`（multipart `cv/ocr/v1/vin_pipeline`）+ `VinPipelineResp` DTO + 双轨鉴权 interceptor（JWT + `X-Gomob-AppId/Sign/Ts/Nonce` HMAC，§14.1）；devserver 挂 `/cv/ocr/` 反代/直挂 cvengine。 | `cv_vin_pipeline` harness 仍过；devserver 起 cvengine 后端侧多 part 请求拿真 verdict；签名校验通过。 | `core/network/.../CVEngineApi.kt`、`server/cmd/devserver/main.go`、`docs/architecture/server/02-api-contract.md` §14.1 |
| M7.6 | `VinCaptureViewModel` + 重写 `ScanCaptureScreen`：删硬编码 `VinValue`，接 `CameraSource` 单帧 RGB 拍照 → `CVEngineApi.vinPipeline(vehicleModelId,image)` → 真 verdict/reasons/字符相似度渲染；`vehicle_model_id` 本期可设/默认值（文档化，待接 catalog 客户端）。 | instrumentation + 真 server 回真 verdict；logcat 见上传；无硬编码 VIN。 | `feature/scan3d/.../VinCaptureViewModel.kt`、`ScanCaptureScreen.kt` |
| M7.7 | 收尾：真机门控（2510DRK44C 直插出帧 + bundle e2e + VIN verdict）；新建/补 `scan_bundle_roundtrip` harness（端打包 → `fusion_service.unpack` → `/fuse` 出 GLB）；更新 finding/registry。 | harness 可判定；真机两功能跑出真实结果（独立门控，受 M6.8b/M1.6 device-gating）。 | `tests/harness/scan_bundle_roundtrip/`、`docs/agent-memory/`、`docs/architecture/registry/` |

## M8 取流/后处理对齐官方 25fps（修 Android 端 ~10fps + 大量延迟）

> 反编译官方 `libBerxelUvcDriver.so` 结论：官方双流稳 25fps 靠 ①event 线程纯 reap+重提交(<1ms) ②后处理(滤波/解码)在专属后台线程(processDepthThread@0xae4f4 / processJpegThreadFunc@0x94b60) ③队列恒 ≤2 帧。
> 我方同一 native 在 **host 跑 45fps**，**Android 仅 ~10fps+延迟** → 瓶颈是 Android 侧特有：显示层死限速 + 后处理与组帧挤在同一 parser 线程 + chunk 队列 1024 太深不丢。
> 主因拆账：「10fps 显示」=UI 死限速(M8.1/M8.4)；「延迟累积到秒级」=单线程后处理 + 深队列不丢(M8.2/M8.3)。
> 关键：host 测不出 `wrap_sys_device`/Android usbfs 行为与显示 fps，M8.2/.3/.5 必须真机验。详见 [[finding_berxel_sdk_acquisition_re_2026-06-03]] + workflow 综合(本会话)。

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M8.1 | ✅已落：depth 显示限速 `DEPTH_PREVIEW_MIN_INTERVAL_MS` 120→33ms(≈8→30fps 上限)，背压靠 SharedFlow DROP_OLDEST。 | 真机：depth `measuredFps` 从 ≤8 升到 native 真实出帧率(改完 M8.2 后 25-45)。 | `feature/scan3d/.../DepthCameraViewModel.kt:153` |
| M8.2 | ★拆 `depth_parser_loop` 为「组帧线程」+「滤波线程」两级：parser 只做 chunk→整帧+marker 分流 depth/IR，整帧推第二级 `filter_thread` 独占跑 `temporal_filter.push`(含 spatial_denoise)。IR plane 与 depth plane 都进 filter_thread 保按序(set_prior_confidence 语义)。对标 processDepthThread。 | host 无回归(depth_seq~45/valid 不降/无崩)；真机 `q_hwm` 稳个位帧、`q_drops≈0`、`measuredFps` 达 25/45、移动跟手 <200ms。 | `native/jni/berxel_dual_session_jni.cpp:508,593` |
| M8.3 | ★组帧→filter 队列以整帧为单位、上限 ≤2-3 帧、满丢最旧；chunk 队列 `kDepthQMaxChunks` 1024 大幅下调。对标官方 ≤2 帧 @0xf0。 | 真机：落后时丢帧而非堆延迟；端到端显示延迟 <200ms(改前秒级)。filter 跳帧靠 motion_reset 容忍。 | `native/jni/berxel_dual_session_jni.cpp:460,484` |
| M8.4 | color 解码移到独立单线程 dispatcher + 自适应背压(上一帧解完才 poll)，再放开 `COLOR_PREVIEW_DECODE_INTERVAL_MS=100`(=10fps 死限)。**先线程化再放限速**，否则全速解码打爆 GC 饿死 depth。终态 TODO：MJPEG 解码下沉 native 异步线程。对标 processJpegThreadFunc。 | 真机：color `measuredFps` 从 10 升到解码上限(≥25)，且 depth 不被饿(depth_seq 不降)。 | `core/native-bridge/.../berxel/BerxelService.kt:814,2992` |
| M8.5 | transfer 池 `kBulkXferCount` 48→20-24 对齐官方水位线(@287618)，**在 M8.2/.3 稳定后做**；保留注释里 48→8 不治死锁的真机证据。 | 真机连续 ≥60s 无新增 `*_err`/babble/掉线(host 测不出 usbfs，必真机)。 | `native/jni/berxel_dual_session_jni.cpp:1104` |
| M8.6 | 终态(按需)：event 线程零分配(per-chunk vector→预分配环形 buffer view)+ 线程优先级/CPU 亲和。当前机型已 PASS，不为未复现瓶颈提前优化。 | 换机出现 reap 落后再做。 | `native/jni/berxel_dual_session_jni.cpp:484` |

## M8 激光扫描设备集成（车辆外廓双单元 LIDAR-PTZ）— ⚠️ 端侧方案已被 M8' 服务端版取代（M8.1 保留休眠）

> 顶栏切设备：激光 = 融合点云（复用 PointCloud3dView）+ 两单元各自点云 + 操作键；Berxel 保持现状。
> 架构（用户拍板 2026-06-03）：Kotlin 网络 + native 几何 + 端侧融合（不上云）+ site-extrinsic 优先/ICP 兜底 + 子网扫描发现。
> docs: docs/architecture/15-laser-scanner-integration.md。设备端点 .101/.102，HTTP :4000，PTS :4010。

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M8.1 | ✅ native `lidar/` 去 PCL 几何内核（Eigen-only）：`lidar_types`(Cloud=`vector<Vector3f>`)、`cloud_build`(lineToWorld 端)、`fusion`(union/randomKeep/cropBox 重写)、`registration`(4-yaw 粗初值，复用 `reconstruction/IcpRegister`)、`scan_vehicle`(reconstructVehicle 编排)、`io_pcd`(最小 PCD writer)；全程 mm；进 `libgomob_native.so`，不引 PCL。 | ✅ `scripts/lidar-host-test.sh` 过：union/keep/crop 计数精确、ICP 复原 180° yaw(误差 0.07mm)、reconstructVehicle ICP/site 双路、lineToWorld 前向链正确，零 PCL 链接通过。 | docs/architecture/15-laser-scanner-integration.md §3-4 |
| M8.2 | native 解帧：端 `scan_stream`(CA FE+CRC-16/MODBUS+zstd 解压)，vendor `third_party/zstd-android`；`NativeBridge.lidarParseFrames(raw,frameType)→FloatArray`(PTS/LDR)。 | host 单测：喂录制原始字节，CRC-OK 帧数、解出点数、首点坐标与 lidar 桌面 `decodePTS`/`decodeLDR` 一致(byte-verified)；`.so` 含 zstd 静态符号、无动态 libzstd 依赖。 | §4 |
| M8.3 | Kotlin `LaserScanner`(core:native-bridge)：子网扫描发现(探 `/api/device_info` 匹配 LTS-T1)、OkHttp 调 `device_status`/`control_scan`(SCAN_START/STOP)、`java.net.Socket` 抓 `:4010` PTS、协程轮询状态门控(SCAN→READY)。 | `tests/harness/laser_capture_sweep/`(真机或回放)：发现两台→触发→Connecting/Scanning/Completed，unitA/B 各拿非空点云；断网 `LaserScanState.Error` 不崩。 | §3,§5 |
| M8.4 | JNI 契约：`NativeBridge.lidarReconstructVehicle`/`lidarLastResult` + `jni/lidar_jni.cpp`(零拷贝 FloatArray)；`core:model` `LidarScanResult`/`LaserStreamFrame`/`LaserScanState`。 | 单测：`lidarReconstructVehicle` 返 FloatArray 长度=3×融合点数，`lidarLastResult().alignMethod` 与传入一致；大数据零拷贝。 | §4 |
| M8.5 | VM 集成：`VehicleContourScanViewModel` 加 `deviceMode`/`laserState`/`switchDevice` + `LaserScanController` 委托；切设备正确 release/acquire/reset。 | instrumentation：切激光→激光体出现、berxel 8 方位环消失；切回恢复；无双源同时持有。 | §5 |
| M8.6 | UI：`DeviceSwitcher`(分段)入 `BackHeader.trailing`；`LaserCaptureBody`(`FusedCloudPanel` 复用 `PointCloud3dView` + `DualUnitCloudRow` + `LaserCaptureBar` 复用 Shutter/RoundSide)。 | `./dev.sh run` + uiautomator：激光/Berxel 分段、融合云 SurfaceView、开始扫描/融合/重来可点；扫一遍融合云非空渲染。 | §5 |
| M8.7 | 端到端真机：.101+.102 双单元扫一台车→native 融合→端侧点云回看；site-extrinsic JSON 从 `core:database`/asset 读，ICP 兜底。 | `tests/harness/laser_scan_vehicle/`：融合点数>单单元和×0.8，alignMethod 命中 site 或 icp 收敛(fitness 达阈)；端侧云可 orbit/zoom。 | §3,§8 |
| M8.8 | 观测闭环：激光链路日志带 unitA/B_ip/frames/align_method/fitness/points_fused/sweep_ms；registry 增 `native/lidar` 模块与 `LaserScanner` 依赖。 | `analyze.py` 输出正常/警告/异常+原因；`registry/modules.yaml`/`dependencies.yaml` 与实际一致。 | §8 |

## M8' 激光扫描下沉服务端（App 瘦客户端）

> 架构变更（用户拍板 2026-06-03）：激光连接/采集/融合全部在服务端；App 只显示+操作。
> 决策：cgo 链 lidar_core.a（带逐帧点回调）+ 半复用（laser_scan_jobs 表 + scan.fusion_done 桥）+ ws 流式推点 + laserworker 同网段。
> docs: docs/architecture/15-laser-scanner-integration.md §9。M8.1 端侧 native/lidar 保留休眠（从 .so 剔除 + DEPRECATED）。

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M8'-C1 | ✅ lidar 仓新增 `src/lib/lidar_scan.{h,cpp}`（C-ABI extern "C"，包 captureSweep+reconstructVehicle，逐帧点回调 on_points/on_status + lidar_scan_cancel）；CMake 出 `liblidar_scan.a`。 | ✅ host 单测 `tests/test_capi.cpp` 过（真机 bin 回放：流式 a=164万/b=250万/fused=414万 与 out 精确一致、status 序列、keep 降采样、cancel 返 2）；✅ **cgo 端到端验证**：Go 经 cgo 链精简 `liblidar_scan.a`(仅 PCL 核心，无 OpenCV/Ceres/VTK/Qt) 调 lidar_scan_replay，流式点穿过 cgo 进 Go。`liblidar_scan.a` CMake 目标已建。 | §9.2 |
| M8'-G1 | ✅ migration `0018_laser_scans`：建 `laser_scan_jobs`（session_key uniq、status[capturing→fusing→done\|failed\|cancelled]、unit_a/b_ip、align/align_method、keep_ratio、三 PCD object_key+calib、pts_a/b/fused/after_crop、b_to_a JSONB、owner_user_id、error）。`pkg/repo/laser_scan.go`：**请求驱动**(Create 幂等 ON CONFLICT / MarkFusing / Complete / Fail / Cancel / FindByID / FindBySessionKey)，非轮询 ClaimNext（live 流式生产者=消费者同进程，见 migration 注释）。 | ✅ `go test ./pkg/repo -run LaserScan` 过：列序纪律守卫(23 列==Scan 目标数)+状态常量与 CHECK 对齐；`go build ./pkg/repo` 干净。 | §9.3 |
| M8'-G2 | ✅ `internal/laser/devctl.go`：控制面 net/http 打 `.101/.102:4000`（GetStatus/GetInfo/ControlScan(SCAN_START/STOP)/Probe），纯解析与传输分离（对照 lidar http_client）。 | ✅ `go test ./internal/laser` 过：真机样本(192.168.9.102_device_status + SAMPLE_device_info)解析、Online 门控、LTS-T1 校验、部分 JSON 不 panic。 | §9.1 |
| M8'-G3 | ✅ cgo 绑定 `internal/laser/cgo.go`：C trampoline(精确匹配 const)→Go //export，**`runtime/cgo.Handle` 把 `*ScanCallbacks` GC 安全穿过 void* user**（替代 PoC 包级全局，支持每会话独立回调）；流式点回调内即拷成 Go `[]float32`。`//go:build laser_cgo` 标签 + 无标签 `cgo_stub.go`（显式返错非假成功）隔离 native 依赖，默认 `go build ./...` 零 native 依赖。`scripts/laser-cgo-setup.sh` 软链 lidar 产物进 `internal/laser/native/`。LDFLAGS=§9.2 已验证集合。 | ✅ 默认 stub 构建+测试干净；✅ `go build -tags laser_cgo` 干净；✅ `go test -tags laser_cgo`(真机 .bin)：流式 a=164万/b=250万/fused=414万 与 out 精确一致、状态 scanning→fusing→done、keep=0.5 严格少、cancel 返 `ScanError{Code:2}`。runner.go 编排归 G4。 | §9.2-9.3 |
| M8'-G4 | ✅ `cmd/laserworker/main.go`（:18087，请求驱动 HTTP 服务，非轮询）+ `internal/laser/runner.go`（编排：累积三朵云 + Sink 实时转发 → MarkFusing → 三 PCD 落 MinIO → repo.Complete → NATS `scan.fusion_done` kind:laser）+ `pcd.go`（binary PCD 编/解码）+ `storage.go`（MinIOCloudStore）+ `sink.go`（natsSink 推 laser.points/status + devctlGate SCAN_START/STOP + deviceProber）。 | ✅ `go test ./internal/laser`（fake 注入，无 cgo/DB/NATS/MinIO）：runner happy/cancel/scan-error、PCD round-trip、完成事件含 kind:"laser"+三 object_key+owner_user_id；laserworker 默认(stub)+`-tags laser_cgo` 双模式构建干净。 | §9.1,9.3 |
| M8'-G5 | ✅ REST handler（POST /v1/scans/laser→201 / {id}/stop→取消 / GET {id} 状态 / **GET {id}/cloud/{name} 流式下载 PCD**，name 白名单从 job 取 key 杜绝穿越）。✅ ws 流式推点：`internal/signaling/laser_bridge.go` 订阅 NATS `laser.points`+`laser.status` 按 owner 路由（镜像 fusion_bridge，已挂 cmd/signaling）。✅ gateway 路由 `{Prefix:"/v1/scans/laser", Target:18087}`（非公开，长前缀优先）。✅ 下载经 laserworker 自有端点（PCD 不入 asset 表，故不走 /v1/assets presign-by-id；融合 414万点不走 ws）。 | ✅ httptest（fake 注入）：201/401/409(单活)/502(探活)/400(align)、stop owner+403、GET、下载 owner/400/403/404；laser_bridge points/status 路由 owner+越权不收+坏JSON；gateway 路由优先级+需鉴权。 | §9.4 |
| M8'-A1 | ✅ `DeviceSwitcher`（段控 激光/相机，放 BackHeader trailing）+ `VehicleContourScanRoute` 重构为 deviceMode 持有者 → `BerxelScanScreen`（原 RGBD body，相机模式才初始化 VM）/ `LaserScanScreen` 分支；LASER 页天然不含 DualPreviewRow/AngleRing/GLB CompletedPanel。 | 复审：rememberSaveable 模式态、唯一调用方 onBack 兼容、各子屏独立 hiltViewModel；无 SDK 不能 instrument，留 E1。 | §9.5 |
| M8'-A2 | ✅ realtime 加 `LaserPoints`/`LaserStatus`/`LaserScanDone`(scan.fusion_done 按 kind 分支) + parser/payload；`LaserScanApi`(Retrofit start/stop/status/downloadCloud)+NetworkModule provider；`LaserScanRepository`(core:data，收口三流 + REST + **PCD binary 解析→FloatArray**，对偶 server pcd.go)；`LaserScanViewModel/State`(Idle→Connecting→Scanning→Processing→Completed\|Error，节流累积两镜头云、done 下载融合 PCD)。 | 复审：Json ignoreUnknownKeys（owner_user_id/calib 被忽略）、FloatArray 序列化、O(n²) 节流(每8帧+尾帧补)、feature 不泄 realtime/network 类型。 | §9.5 |
| M8'-A3 | ✅ `LaserScanScreen`：`LaserCaptureBody` = 融合 `PointCloud3dView`(上58%) + 两镜头并排(下42%) + `LaserControlBar`(开始/停止/重扫，态随 state)；空态提示、点数角标、均值 z 居中取景(remember memo)。复用 PointCloud3dView(mm)。 | 复审：主题 token(spacing/shapes/colors)全部存在、import 干净、PillButton/状态机分支。真机 UI 留 E1。 | §9.5 |
| M8'-E1 | ✅ **真机 live 服务端路径已验证**：真 Runner（真 devctl 门控 SCAN_START/STOP + 真 cgo LiveScan + fake 存储/repo + 记录 Sink）对真 .101/.102 跑受控扫描。`live_real_test.go`(laser_cgo + env 门控)。 | ✅ 探活两单元 READY/LTS-T1；live 扫描 status=done，**融合 154074 = unitA 85492 + unitB 68582(union 精确)**，582 流式帧穿 cgo 进 Go，状态 connecting→scanning→fusing→done，23.3s；设备扫后复位 READY。**剩**：真 MinIO/NATS/DB/gateway 起栈 + App 真机渲染（需 Android SDK，本环境不可；server 全链已证）。 | §9 |
| M8'-D | ✅ **devserver 并入激光路由 + laser_bridge**（commit fde8b7a）：devserver 是 App 在 dev 唯一连的 all-in-one 网关，此前只接了 cmd/gateway+cmd/signaling 的生产式拆分、**漏接激光**。补 `newLaserProxy`（反代 `/v1/scans/laser[/*]`→:18087，**挂 top-level mux 自包 `auth.Required`**——不能进 protected mux，否则与 asset 的 `/v1/scans/{sk}/result` 在 `/v1/scans/laser/result` 路由歧义 panic）+ `startLaserBridge`（NATS `laser.points`/`laser.status`→ws，镜像 startFusionBridge）。 | ✅ 真机：App→devserver→laserworker→.101/.102→NATS→ws 全链跑通（见 E2）。 | §9.4 |
| M8'-E2 | ✅ **全栈端到端 through-App 真机验证完成（2026-06-04）**：起 podman 栈(pg/redis/nats/minio)+migrate 0018+`devserver(:18808)`+`laserworker(:18087,-tags laser_cgo)`；App 在 Android 模拟器(emulator-5554,-gpu host,VNC :1)真编译真装。登录 shenhm/shenhm123→3D 页→车辆外廓→激光段控→开始扫描→`POST /v1/scans/laser` 201→**点云实时流进镜头 A/B 面板**→`scan.fusion_done` 经 ws delivered=1→`GET .../cloud/fused` 200 下载渲染。 | ✅ 全链 delivered。**环境坑**：模拟器 10.0.2.2 QEMU netsim 不稳（POST 登录 "unexpected end of stream"）→**必须 `adb reverse tcp:8808 tcp:18808`+`tcp:18808 tcp:18808`**，App 端点 127.0.0.1:18808。见 [[gomob-android-emulator-env]]。 | §9.4 |
| M8'-A4 | ✅ **融合视图自适应取景修全黑**（commit 7297314）：`PointCloud3dView` 加 `autoFit`（首帧非空按包围球拟合 target=质心/distance=R÷sin22.5°/far 随扩，仅融合面板开）。**align 默认 icp→none**：ICP 跨单元对固定双机位不稳、空场即发散（实测把 B 甩到 Z −6750..10260=17m）；固定基线正解=site 标定外参（`out_live/site_extrinsic.json` 存在，但单位存疑+laserworker 未把 SiteJSON 接进 RunSpec→align=site 必回退 none，**补 site 接线是明确后续**）。 | ✅ 真机融合面板正常显示。 | §9.5 |
| M8'-F1 | ✅ **服务端设备控制面**（commit 230c986）：`devctl.go` 扩 `DeviceInfo`（specs+`Control ControlSettings`+`Calib CalibParams`），新增 `UpdateControl`（POST `/api/update_control` {"control":s}）/`UpdateCalib`（POST `/api/update_calib_parameters` {"parameters":p}）/`postJSON`，`ParseDeviceInfo` 解析 `control`+`parameters` 节点。`handler.go` 加 `DeviceAPI` 接口 + `resolveUnit(?unit=a\|b\|101\|102)`，注册 5 路由 `GET/POST /v1/scans/laser/device-{status,info,command,scan-settings,calib}`（literal 子资源+?unit= 查询，避开 wildcard `{id}` 路由冲突）。**DeviceCommand 白名单 = ScanStop/ScanWatch/AlignZero/ClearError/SoftReboot**（不含 ScanStart——起扫走正式 job）。 | ✅ `go test ./internal/laser`（fake DeviceAPI 注入）：5 端点 200/鉴权/?unit 解析/命令白名单拒非法 cmd。 | §9.7 |
| M8'-F2 | ✅ **App 设备控制面板——原厂功能键**（commit c2bdf5f）：`LaserScanApi` 加 5 端点+DTO（LaserDeviceStatus/Info/ControlSettings/CalibParams/Lidar/CameraCalib/Body2World，@SerialName 对齐 server JSON、数组 List<Double>）；`LaserScanRepository` 加 deviceStatus/Info/Command/updateScanSettings/updateCalib + 领域类型 DeviceStatusInfo/DeviceFullInfo/ScanSettings/DeviceCalib + toDomain/toNetwork 映射；新文件 `LaserDeviceControl.kt`（`LaserDeviceViewModel` + `LaserDeviceControlSheet` ModalBottomSheet：UnitTabs/StatusSection/控制 FlowRow/ScanSettingsSection/CalibSection(可折叠,逗号分隔数组)/DeviceInfoSection + **ConfirmDialog 二次确认弹窗**）。`LaserScanScreen` 顶栏加齿轮按钮唤起。**破坏性操作（软件复位/标定写入）走二次确认弹窗**（用户拍板）。 | 复审：状态/控制/扫描设置/标定/信息四组齐；无 SDK 留真机。真机：面板拉到 .101/.102 真实 status+info。 | §9.7 |
| M8'-A5 | ✅ **激光页交互打磨**（commit 2635cd7，用户反馈）：①设备面板 `selectUnit` 切镜头**不再清空旧 status/info**（保留当占位，新数据原地替换，避免页面收缩）；②镜头 A/B 开 `autoFit`+`PointCloud3dView` 改**每帧重拟合**（相机随点云生长扩展取景、纳全部真实点、只缩放跟随保留旋转）、**纯原始点直渲不处理**、节流 EMIT_EVERY 8→2 更实时；③底栏改**相机式三键 撤销｜开始扫描｜完成**（新 `LaserSideButton` 镜像相机 RoundSideButton）：**撤销**=`undo()` 停扫+清空两镜头/融合云+两单元 ALIGN_ZERO 镜头归零；**完成**=`onBack` 离场（结果已存服务端）。 | ✅ 新三键底栏真机上图验证渲染正确；镜头 autoFit 实时直渲待 .101 恢复后真扫验证。 | §9.5 |

## M9 车辆外廓测量/建模层（消费融合点云 → 尺寸数字）

> 漏掉的「应用软件」层：采集/融合（M8'）只到融合点云，原厂 JCHY 在其上做**车型建模 + 外廓测量**（对标 GB7258-2017 §4.15）。
> 逆向真理源：`docs/architecture/16-jchy-vehicle-measurement-app.md`（车型表/carType 解密/8 阶段管线/测量字典/§10 未坐实点）；finding `docs/agent-memory/finding_jchy_measurement_layer_re_2026-06-04.md`。
> 真值基线：原厂会话 `/root/WindowsR/JCHY_OFFLINE/Data/100742/`（`1.pcd`/`2.pcd` + `Result.ini`：L=1777/W=533/H=759，L2=1775/W2=534/H2=761，总轴距 1370、前后悬 261/163，carType=2）。
> 几何已离线验证（ROI 裁剪→最大连通簇→半径离群→俯视 minAreaRect OBB + Z 包围盒）：两镜头 LWH 复算 ≈±1~2%。
> 架构：测量内核先做 host 可测 Eigen-only C++（镜像 `native/lidar`），终态接 laserworker（cgo C-ABI，融合后调用），不引 PCL。
> docs: docs/architecture/16-jchy-vehicle-measurement-app.md §3/§5/§9

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M9.1 | ✅ **测量管线骨架 + 预处理**：`native/measurement/`（Eigen-only）：`preprocess`(ROI 裁剪复用 `lidar::cropBox`[Param] x[270,1000]/y[0,2200]/z[10,800] + `voxelDownsample` + `radiusOutlierRemoval` 网格近邻 + `largestEuclideanCluster` 体素 26-连通 BFS)；`io_pcd` 补 `loadPcd`(XYZ/XYZRGB binary 读)。 | ✅ `scripts/measurement-host-test.sh` 过：minAreaRect 旋转不变、合成盒 ROR 保稠密主体/剥稀疏飞点、`largestEuclideanCluster` 主簇==车体点数(脱离 blob 全剔)、`loadPcd` 往返一致；零 PCL 链接；`lidar-host-test.sh` 回归不破。**坑**：ROR 半径15/邻居12 按真实云~1mm 密度调，合成盒须 5mm 采样否则误删(已注释)。 | §3②, §6.3 |
| M9.2 | ✅ **LWH 几何测量**（OBB 俯视 + Z 包围盒）：`dimensions`(`minAreaRectXY` 角度扫描 0.25°→长边=车长/短边=车宽)、Z 跨度=车高；`measure_vehicle` 编排 预处理→LWH。 | ✅ harness `tests/harness/vehicle_measure/` 对 `100742` 真机云→**结论 正常**：1.pcd L1797/W523/H750 vs 1777/533/759（+1.1/-1.9/-1.2%）、2.pcd L1770/W523/H755 vs 1775/534/761（+0.3/-2.0/-0.8%），最大 2.0% ≤2.5%。**<1% 待反汇编 `bound_box` 6 int + SOR k/std + carType Type2 偏移**（§10）→ M9.2b（W/H 系统性偏小 ~10mm 是已知残差）。 | §3⑥, §5 |
| M9.2b | **LWH <1% 对齐**（消 §10 缺口）：反汇编 `AlgFuncDLL`/`JCHY.pdb` 的 `bound_box`/`setstandardValue` 取精确 SOR/百分位/carType `_x/_y/_z` 消费路径，补齐当前系统性 ~10mm（W/H 偏小）残差。 | 两镜头 LWH 对 `Result.ini` 误差 <1%；偏移来源在文档 §10 由 [推断] 升 [直证]。 | §10 |
| M9.3 | ✅ **车型目录 + carType 参数表（数据层）**：`native/measurement/vehicle_catalog.{h,cpp}`：①26 车型枚举（货车 0-15 / 挂车 50-59，含中文名 + 货车/挂车分组 + 罐体/吊车标志，§4.1）；②carType **XOR 编解码**（周期16回文 key 自逆 `applyCarTypeXor`）+ `parseCarTypeOffsets` + `loadCarTypeTable` —— **直接读原厂 `carType.ini`**（真实数据源，不硬编码副本）。 | ✅ `measurement-host-test.sh` 加 `vehicle_catalog_test` 全过：XOR 自逆往返、明文解析、**真机 `carType.ini` 解出 31 条目且 Type0/2/51/Tank5/59/board/Type2_s 值精确**、26 车型目录(缺号 16/60 不存在、分组/罐体/吊车标志)。 | §4 |
| M9.3b | **车型几何判型规则**：`getVehicleType/getTrailerType/BoxOrCangShan` 几何规则（投影外形/长宽比/分段/罐体检测）+ 判型后叠 carType 偏移（消费语义见 §10）。⚠️ **当前阻塞**：仅 100742 一个标注样本（carType=2），单样本无法拟合/验证多分类规则；PDB↔exe(JCHY_new) 构建不一致使纯反汇编低 ROI。需多标注原厂会话 或 反汇编对齐的构建。 | harness：≥10 个不同车型标注会话，判型与 `Result.ini carType` 一致率 100%（常规/牵引/平板先行，罐车走 M9.5 `CompareCloudType5/7`）。 | §3④, §4, §10 |
| M9.4 | ✅ **轴距 + 前后悬**（几何贴地接触带轮检测，`axle.go`）：物理判据=轮是唯一触地部件 → 贴地接触带(离地高<contactH)沿 OBB 车长轴的点密度峰=轴心（车厢侧壁/底盘悬在离地间隙以上、不进接触带，自然排除）；`findPeaks1D`(高度阈+突出度+最小间距)取轴心→相邻间距=轴距、两端伸出=前后悬。**关键陷阱**：轴心检测须跑在 裁剪后/聚类前 的点上——`largestCluster` 会把悬挂间隙>体素而与车体不连通的轮(尤其驾驶室下前轮)当独立小簇丢掉，使该轴接触峰塌到阈值下；`measureBody` 故返回 `roiPts` 给 `DetectAxles`，LWH 仍走主簇→ROR。`MeasureFull` 同帧出 LWH+轴距；接入 runner `axle` stats + `FusionDoneEvent` + 网页测量面板(/latest 拍平历史)。 | ✅ harness `tests/harness/vehicle_axle/` 对 `100742`→检出 4 轴，轴距 744/404/263 vs 710/399/261、前悬250/后悬134，结论⚠警告(轴距1 4.8%，在 6% 达标线内)。✅ `go test ./internal/laser TestAxle_*`：真值 744/398/269、合成 4 轴 31°旋转下 700/400/300 精确(证旋转无关)。**后悬最弱(~18%)受尾端噪声**；真车收紧待设备扫描重标 contactFrac/阈值。 | §3⑥, §5, §10 |
| M9.4b | ✅ **货箱测量**（几何分割+外尺寸，`cargobox.go`）：物理判据=货箱是车尾侧"顶高接近全局最高"的最长连续段(车头顶矮+常有缝)；箱顶 rim(顶部薄层)取外长/外宽(rim 干净无轮)；箱壁竖直→横截面宽度随高度恒定，**最长恒宽 z 段=箱壁、其底=bed**，箱深=箱顶-bed(≈栏板深度)；内宽=壁带最靠中心两壁峰间距(壁中距，厚壁分辨率受限=参考值)。`MeasureFull` 同帧出 LWH+轴距+货箱；接 runner `cargo_box` stats + `FusionDoneEvent`(box_outer_length/width/depth) + 网页面板 + /latest 拍平。 | ✅ harness `tests/harness/vehicle_cargobox/`：**合成"车头+开顶货箱"已知尺寸闭环**外长1007/1000 外宽600/600 箱深454/460；`100742` 分割 sanity 货箱占车长59% 外长1057 外宽466 箱深455(与 rim/EDA 一致)。✅ `go test TestCargoBox_*`：合成 3 旋转角(0/27/63°)外长1020/外宽600/箱深450 旋转无关、100742 sanity 全过。**100742 无货箱数值真值**(carType=2 未触发箱测)→ 数值闭环靠合成；真车箱尺寸待设备扫箱车验证。内宽厚壁不准=参考。 | §3⑥, §5, §10 |
| M9.4c | ✅ **融合后自动分割测量 + 网页可视叠加**（`overlay.go` + web）：融合完成即自动跑分割/测量(本就服务端同帧算)，并把结果"看得见"。① **世界系叠加几何**：`BuildVehicleOverlay` 把 车体框/货箱框/各轴线 按融合云世界系导出(8 角点/两端点)——测量在变换帧 M(车位框/地面/设备系)里算，求 **M→世界仿射逆**(三帧统一 `world=O+m·(A0,A1,A2)`：设备恒等、地面 toGroundFrame⁻¹、车位框 toBoxFrame⁻¹)把几何映回，OBB 对齐帧 `m=l·Ldir+w·Wdir+h·ez`。车体框用 cleaned(主簇→ROR)与测量 OBB 一致，轴/货箱用 roiPts。② 接 runner `overlay` stats + `FusionDoneEvent.overlay` + handler /latest。③ **网页**：`renderVehicleOverlay` 在融合视图 markerLayer 画 SVG(车体蓝/货箱黄/轴红 12 棱+轴线，`projectPointInPane` 投影，渲染器无关)；测量面板**融合后总是出现**(测得出显数字+图例，测不出给原因:未标定/需圈车位框)。 | ✅ `go test TestOverlay_*`：合成车头+货箱 3 旋转角(0/27/63°)车体框棱 1580/600/760、货箱框 1020/600/450 **旋转无关**；合成 4 轴叠加轴线数=4 横跨≈车宽；`100742` 车体框 1795/545/755(=测量 OBB)、货箱框棱[1080/467/452]、4 轴线、角点全落融合云包围盒内；事件 JSON 字段名(overlay/vehicle_box/cargo_box/axle_lines)校验过。**剩**：真机端到端视觉复核(网页叠加投影对位，设备扫描门控)。 | §3⑥ |
| M9.5 | **专项车型测量**（罐体/栏板/护栏）：`tankprocess` 罐体三段切分（前/中/后=长×直径1×直径2）+ 倾斜角 + 容积（填充系数 `vol_s=0.96`）；`processbaffle` 栏板深度 + 四向护栏离地高（地面基准拟合）；`CompareCloudType5/7` 罐车模板比对。 | harness（需补罐车/栏板车样本——当前 100742 是常规车全 0）：三段尺寸/容积/栏板深度/护栏离地与对应原厂 `Result.ini` 对齐；无样本前不算完成。 | §5, §10 |
| M9.6 | ✅ **服务端接线 + 合规（融合后自动测量）**：`server/internal/laser/measure.go` —— **纯 Go 镜像**已验证 C++ 内核（ROI→簇→ROR→OBB+Z），`runner.Run` 融合后对 fused 云 `Measure` + `CheckCompliance`([LIMT] 车长≤12000/宽≤2550/高≤4000)；结果入 `laser_scan_jobs.stats` JSONB（measure/compliance，**免迁移**）+ `FusionDoneEvent` 加 length/width/height/measure_valid/compliant/violations（经 laser_bridge ws 推端侧）。测量无效非致命（不让 job 失败）。**为何 Go 不 cgo**：测量是 PCL-free 纯几何，无需 cgo；放 Go 让所有构建可测且 `go test` 即对真值验证；C++ 内核仍是 native/Android 参考（pcd.go 镜像 C++ io_pcd 同范式）。大云优化：OBB 角度在抽样子集(≤12万)搜、全量算精确跨度。 | ✅ `go test ./internal/laser`（默认无 cgo/DB/NATS）：**真值 Data/100742 L1797/W523/H750(1.1/1.9/1.2%)、L1770/W523/H755(0.3/2.0/0.8%) 均 ≤2.5%**（与 C++ harness 逐位一致）；融合 union L1795/W545/H755；合成盒旋转不变+脱离 blob 剔除+合规 3 项；runner happy/cancel/error 全过(测量非致命)；`go build ./...`+vet+`-tags laser_cgo` 编译干净+signaling 不破。✅ **App 测量卡片**：`FusionDoneEvent` 加 length/width/height/measure_valid/compliant/violations → `RealtimeEnvelopeParser`(+2 单测过) → `LaserScanRepository.VehicleMeasurement` → `LaserScanState.Completed` → `LaserScanScreen.MeasurementCard`（融合云上叠车长/宽/高(米)+GB7258 合规徽章/超限项；无效则提示不可用）；`:core:realtime/:core:data/:feature:scan3d` 编译干净。**剩**：真机端到端可视复核（受 Android SDK / .101 恢复门控）。 | §9.2, §8 |
| M9.7 | ✅ **真机扫描链路修复（2026-06-04，断电复位激光后逐项坐实）**：① **离群垃圾点过滤**——设备偶发吐坐标爆表(~1e37mm/NaN)的无效 PTS，`lidar/handlePts` 加 `ptsPointValid`（finite + \|v\|≤50m）源头剔除；App `PointCloud3dView.fitTo` 加 isSane 抗离群兜底（垃圾点曾把 autoFit 质心拉到 1e31mm 外致面板全黑=用户"完全不对"真因）。② **渲染上限** 30万→80万 + 超限均匀 stride 抽样（融合云=A全部+B全部拼接，旧截尾丢整个 B）。③ **空扫守卫**（runner h_angle 跨度<10° fail-fast）正确拦下两次坏扫描。④ **A 不扫根因**=gate 起扫前 `UpdateControl` 改角度紧贴 `SCAN_START` 把 A 弄瘫（A 移到起始角即 READY 不进 SCAN，与角度值无关；-180/180 还撞 start==stop mod360）→ 关 `SET_SCAN_ANGLES`，用设备持久化可扫角度（A:0-90/B:-180-20）。 | ✅ job 12 真机 PASS：A 真扫 0→90° 310k 点 / B -180→20° 274k / fused 585k，三面板正确取景；离群 0.002% 由 App+服务端双层兜住；lidar 12 ctest 全过。**注**：~13/585207 杂点疑 ~1MiB 缓冲边界注入（非过滤漏，0.002%）单独挂账；测量 ROI(270-1000/0-2200/10-800)不匹配真机坐标系致 roi_pts=0（待按真实底座重标）。 | docs/16 §8 |
| M9.8 | ✅ **点云视角器：地面检测 + 视角预设 + 地面网格（2026-06-04，用户体验）**：① 服务端 `ground.go` RANSAC 拟合地面（**重力先验**=只接受法向与设备 +Z 夹角≤35° 的近水平平面，破整屋扫描里墙/地内点数相当时误选墙）→ 法向(上)+offset 入 stats + `FusionDoneEvent`(ground_n*/d/valid)。② 数据契约 realtime→core/data `GroundPlane`→`LaserScanState.Completed`。③ App `PointCloud3dView`：相机 up 改用地面法向（摆正设备世界系 ~6.8° 竖直倾斜）、orbit/预设相对地面正交基；**顶/侧/等距/自由**视角预设（融合面板右上 `ViewPresetBar` 段控一键切机位）；地面平面叠半透 **LINES 参考网格**（~0.5m 间距，按质心投影+包围球范围）。④ **交互精修（2026-06-04，模型查看器范式）**：等距→**斜**重命名（`ISO`→`OBLIQUE`）；**顶/侧/斜=锁俯仰仅绕地面"上"轴转台旋转(yaw)**，**自由=全向轨道(yaw+pitch)**；双指=捏合缩放 + **双指平移**（midpoint 位移沿屏幕基反向移 target，顶视用 `cosY·right−sinY·fwd` 水平基不退化）；新增 **重置**键（`resetSignal`→`resetView`：回当前预设家位 + 清平移 + 按包围球重设距离）。 | ✅ host `ground_test`（合成水平/倾斜5.6°/退化/抗垃圾点）+ 真机 job12 重力先验正确选地面 法向(0.119,-0.011,0.993)≈+Z(倾6.8°)/valid/13.7% 内点（对照无先验误选 X 法向墙面）；laserworker new6 + APK 编译装机。**交互精修验证(job16, fused 598980)**：App 发起→Completed，`ViewPresetBar` 顶/侧/斜/自由/重置 全渲染；连点全预设 + 重置 + 单指转台/俯仰拖动 0 崩溃 0 Filament 异常。**剩**：用户 VNC 手势手感复核（双指缩放/平移跟手、摆正、网格）。 | — |
| M9.9 | ✅ **激光页重构为模型查看器范式（2026-06-04，用户体验）**：弃"上大融合 + 下两小镜头"三窗口，改 **单主点云窗口（复用一个 Filament view）+ 缩略图切换行 + 状态结果区**。① 主窗按 `LaserCloudKind`(FUSED/A/B) 切换喂云，仅 FUSED 挂地面网格 + `ViewPresetBar`，A/B 自由轨道(+Z up)。② 缩略图行=3 个 **2D Canvas 散点缩略图**(`project2D` 取 range 最大两轴正交投影、stride 抽样 ~1500、抗垃圾点)，**不开 3 个 Filament 引擎**(软渲染会卡爆)，采集时实时长、点击切主窗、选中描边高亮。③ **自动切换**：起扫且融合空→看镜头 A 实时；融合完成→自动切 FUSED；回 Idle 复位。④ **状态结果区**独立成块（状态行 spinner/状态点 + 完成计数 + 测量卡常驻，从悬浮改常驻；控制栏去重计数）。 | ✅ 模拟器 job18 端到端：Idle→起扫**自动切 A 实时(155054 点 + 缩略图实时散点)**→完成**自动切融合(595071)**+段控齐+状态区"扫描完成…"+测量提示；三缩略图点击切主窗(A/融合/B 标题正确)0 崩溃；编译 + APK 装机 + 4 张截图复核布局正确。**剩**：用户 VNC 观感确认。 | — |
| M9.10 | ✅ **测量改地面相对、坐标系无关（2026-06-04，根治 roi_pts=0）**：根因双重——① 硬编码 JCHY ROI `x[270,1000]y[0,2200]z[10,800]` 在 JCHY 离线数据坐标原点，真机底座融合云 bbox `x[-14047,4307]y[-18079,3640]z[-18098,3088]` 与其**零重叠**→`roi_pts=0`；② 真机这次扫的是整间房（18m 跨度、最大平面是墙、无车）。**方案**（用户拍板"先改对算法+真值验证"）：`measure.go` 加 `GroundMeasureParams`/`UseGround` 路径——`toGroundFrame` 把云变换到地面正交基(x=右/y=前/z=离地高)→高度 band `[hMin,hMax]` 替代设备 ROI→俯视 `minAreaRectXY` 出长/宽、`maxZ`(离地最高)出车高，**全相对真实地面、换任何底座坐标系都成立**；`runner.go` 地面有效时走此路径(hMin30/hMax5000)，否则回退设备 ROI。**JCHY 设备 ROI 路径一字未改**（验证基线不动）。 | ✅ `go test ./internal/laser`：**JCHY 真值设备路径** 1.pcd L1797(1.1%)/W523(1.9%)/H750(1.2%)、2.pcd 全<2.5%（未退化）；**新地面路径在 JCHY 真值上** 1.pcd L1797/W523/H760、2.pcd L1770/W523/H765 全≤2.1%（证无侧向框靠最大簇也能隔出车、与设备路径等效）；**合成倾斜地面盒**(绕X12°/Y8°+任意平移) L1800.7/W532.4/H760.0 vs 1800/530/760 **<2.5mm**；**job19 真机帧** roi_pts 0→valid 测出主离地簇(L3090/W367/H1776=房间墙/门框,符合无车事实)。devserver/laserworker 重建(new7)起，go vet 净。**剩(用户已延后)**：扫描区放真实目标物(车/已知尺寸大件)重扫 live 验出数；整屋杂场景需侧向车道框排除墙(部署期再定)。 | docs/16 §3⑥/§6.3/§8 |
| M9.11 | 🟡 **持久 3D 裁剪框（车位）→ 软件裁剪 → 框内测量（2026-06-05）**：用户要"圈点云范围→反算扫描角"。**真机实测推翻反算扫描角这条路**（scan24 数据）：设备只有 pan+俯仰两个角度闸门、**无深度闸门**(`lidar_filter_zone`=竖直角非距离)；把扫描限到车的 pan×俯仰立体角后远处办公室点仍保留 **99.3%**——背景藏在车的同一视线立体角里，任何扫描角组合都分不开；唯一能按深度隔离的是 **3D 框软件裁剪**。两单元螺丝固定→框定义在 unit_a/融合世界系即跨扫描稳定，**不依赖不可靠的自动地面 RANSAC**（scan24 又拟到天花）。**实现**：① 服务端 `cropbox.go`(OBB: center/up/yawDeg/half，`Basis`/`CropToBox`/`toBoxFrame`) + `measure.go` `CropBoxMeasureParams`/`UseCropBox` 路径(框局部系裁剪→主簇→ROR→俯视OBB)；② migration `0019_laser_crop_box`(bayKey=unit_a_ip→box jsonb) + `repo.LaserCropBoxRepo` + `laser.DBCropBoxStore`；③ API `GET/PUT /v1/scans/laser/crop-box` + `POST .../{id}/crop-preview`(给定框裁某扫融合云返点数+测量，供拖框预览)；④ `runner` 测量优先级 持久框 > 自动地面 > 设备ROI；⑤ App `LaserCropBoxEditor`(顶视拖框 footprint+高度带+up翻转+yaw+实时点数/临时尺寸+保存) 接 `LaserScanScreen`；⑥ `parsePcdBinary` 修 XYZI 回归（单元云改 16B/点）；⑦ **入口收进设备控制 sheet**（`LaserDeviceControlSheet`「测量范围」节，一次性标定不占主操作流）——进场预载持久框显示"已圈选"；有完成态融合云方可点选圈框，无云显式提示"先完成一次扫描"（不造假可点），点击关 sheet→开顶视编辑器；⑧ **编辑器布局重排**（用户体验）——顶视点云 `BoxWithConstraints` 占满整屏；调整控件改成**底部可显隐浮层**（顶部常驻 `OverlayPill` 取消/轴向/「调整▴/收起调整▾」切换；面板内读数固定顶、滑杆中段限高滚动、翻转/保存固定底常驻——保存不再被折叠在滚动区）；⑨ **可操作性修复**（实扫复核发现默认框居中包围盒几何中心、点偏一侧→框内=0、黄框跑出视野）——默认框改 `fitToCloud`（各轴 2–98 百分位自动套住点云主体，框内开场即>0、黄框必在视野）；点更亮更大(out 0xFF8A9BBD/3.2f、in 0xFF5BD6FF/4.5f)+框线加粗+**框心黄环标记**(拖动目标可见)；加 **「套框」键**(拖飞一键拉回拟合)；⑩ **改直接手势画框**（用户拍板：滑杆调框太笨）——`TopViewCanvas` 手势分流(`DragMode` MOVE/RESIZE/ROTATE)：拖框体平移、拖四角缩放(绕心对称)、拖顶部 ✛ 旋转柄转向，命中判定与缩放都在 (u,v) mm 基坐标算(与服务端一致)；画 4 角手柄+旋转柄+连接线+框心；宽/长/yaw 滑杆退场，仅留顶视拖不了的 框高+离地中心 两滑杆→面板更小点云更大。 | ✅ `go test ./internal/laser`：cropbox(基/裁剪/toBoxFrame/Valid) + handler(get/put/preview/退化/鉴权) + **真机 scan24** 框裁 594282→164039(28%) 测量有效被框约束；`PcdParseTest`(XYZ/XYZI 双解析)；migration 0019 应用版本 19；laserworker new9 重建重启，**curl 端到端** GET(false)→PUT→GET(true)→preview(594282→119846 valid) 全过；**模拟器端到端实扫**(scan32/33 fused 59.8万点)→设置「测量范围」转可点→开编辑器→新布局(全屏点云+顶部切换+底部浮层)+显隐切换(收起→点云全屏/展开恢复)+保存常驻可见 全复核通过，0 崩溃；**手势版实扫复核**(scan35 fused 53万点)：默认 fitToCloud 框内 3580、adb 模拟拖框体(框内变)/拖角(裁后 533207→564930)/拖旋转柄(裁后→512066,框可视倾斜)/套框(回 3580/533207) 全生效，4角+旋转柄手柄渲染正常，0 崩溃；**端到端闭环模拟器全程跑通**：保存框→`PUT /crop-box` 200→DB `laser_crop_box` 持久(bay_key=192.168.9.101)→设置「测量范围」转「已圈选」→重新扫描(scan36)→stats **`measure_mode` 由 ground(scan35 roi 673)翻成 crop_box(scan36 roi 499620)**、`vehicle_type_id:2`+`car_offset:[-20,-35,-10]` 叠加、compliant，证 **crop_box>ground>device_roi 优先级 + 车型偏移 + 持久框跨扫描** 全链生效（测试框已从 DB 清除避免污染真车）。**剩**：真车现场拖框复核手感（空办公室点稀疏看不出真实操作体验）。 | docs/16；docs/agent-memory/finding_laser_scan_angle_cannot_depth_isolate_2026-06-05.md |
| M9.13 | 🟡 **空工位背景相减 → 全自动抠车（路 B，2026-06-17）**：根因——真机融合云=整个房间(scan168 实测 X/Y/Z 各~3m)，车跟房间(地/墙/天花/杂物)连成一坨，纯几何抠不开；device_roi(vendor 设备系 ROI)对世界系融合云无效裁出薄片(L=135)、ground 量成房间(L≈3159 H≈3103)、DB 无车位框 → 把房间当车显示。原厂 JCHY 亦靠**固定标定 ROI 框**(docs/16 §6.3 PassThrough，`1.pcd` 点全落 [270,1000]×…)非自动分割。**解法路 B**(用户拍板)：固定安装→扫一次空工位当背景→`SubtractBackground(live,bg,tol)`(体素哈希+27邻域，`background.go`)剔除静态房间=车。① 背景**点云存 MinIO**(稳定 key `laser-scans/background/<bayKey>/fused.pcd`，非 postgres JSONB)；② 采集=扫描请求 `mark_as_background=true`→runner 存背景跳测量、事件 `background_captured`；③ 测量优先级 `crop_box>bg_subtract>无隔离→measure_valid=false`(诚实闸，去掉 device_roi 误测)；④ 网页顶栏"采集空工位背景"按钮+状态小标+无背景时面板提示"需采集背景"；⑤ `GET /v1/scans/laser/background` 查状态。 | ✅ `go test ./internal/laser`：`TestBackgroundSubtract_RecoversVehicle`(合成房间+车 18°旋转→减出车 L=4000/W=1800/H=1440、前景/车=0.97) + `TestRunnerBackgroundSubtractWiring`(采集→存背景 key→读回→bg_subtract→前景26949≈车28064、远<融合99888，整链通) + 既有 laser 全过；vet 净；全量 `go build ./...` 通过。**harness `laser_background`** 扫 tol 实证 **tol 须>噪声+漂移**(±15mm 下 tol≤20 残留背景、≥30 干净、默认40有余量)→✅正常。web JS 语法过、`?v=20260617-bg-subtract`。**剩**：真机扫一次空工位+一次有车端到端复核背景相减 + 叠加对位(设备门控)；tol 按真机噪声重标。 | docs/16 §6.3；docs/agent-memory/finding_vehicle_isolation_bg_subtract_2026-06-17.md |
| M9.12 | 🟡 **激光页布局精修 + 车型下拉（2026-06-05，用户体验）**：① 点云区压缩腾空间——状态上移点云左上角徽标(`CloudStatusBadge`)、删底部常驻状态卡(仅完成态渲染结果卡)、缩略图行 aspectRatio→固定 54dp、测量数字不缩(米值 17sp)只压留白、控制栏压扁；完成态点云区 +~150dp/采集态 +~190dp。② **点云视角不再被增量更新重置**——`setPoints` 原每帧无条件 `fitTo` 冲掉手动视角；改 `userInteracted` 标志：未上手时跟随云生长、一旦 orbit/缩放/平移即冻结，切云(`autoFitKey`)/重置/新扫复位才恢复跟随。③ **控制栏布局互换**：撤销键→**车型下拉选**(`VehicleTypeDropdown`，逆向 JCHY 26 型按货车/挂车分组)、完成键→**设置键**(开设备控制)、标题栏齿轮去重(设置下移)。④ **车型贯通**：`core:data VehicleTypeCatalog`(26 型单一真理源) → VM 选中态 → `start()` 携 `vehicleTypeId` → `LaserScanStartRequest`/`startReq` → `RunSpec` → **carType 偏移**(`cartype.go` Go 镜像 docs/16 §4.3，叠到设备ROI/裁剪框测量中心，地面路径暂不接) + **按型合规钩子**(`LimitsForVehicleType`，逐型限值未核验→回退通用值+TODO 指 GB7258 类别表，不编造) + stats 落 `vehicle_type_id`/`car_offset`。 | ✅ `go test ./internal/laser`：`TestCarTypeOffsetTable`(偏移表值) + `TestCarOffsetShiftsCropBox`(偏移平移框心纳入框外点) + 既有 cropbox/handler/真机数据全过；vet 净；App `:core:network/:core:data/:feature:scan3d` 编译干净；laserworker new10(cgo) 重启 healthz ok；APK 装真机 2510DRK44C 0 崩溃启动。**剩**：真机点选复核(视角锁定手感、车型下拉分组选择、设置键开设备控制)；逐型合规限值表录入(M9.2b)；carType tank/board 子变体运行时分类。 | docs/16 §4.1/§4.3/§8 |

## M10 激光点云操作界面重做 + 第一视角漫游标注（2026-06-05，用户体验）

> 用户反馈"点云操作界面还是很难用"。三决策（结构化确认）：① A/B/融合三窗同款轨道；② 标注车位框走第一视角漫游"走一圈"拟合 OBB；③ 车位框按镜头各存独立框。
> docs: `docs/agent-memory/finding_laser_roam_percamera_cropbox_2026-06-05.md`

| ID | 任务 | 验收 | 文档 |
|----|------|------|------|
| M10.1 | ✅ **统一 A/B/融合操作窗口**：移除"仅融合给视角预设条"限制，`viewPreset` 应用到三窗、凡当前云非空即显示顶/侧/斜/自由+重置；去掉非完成态强制 FREE 的 gate。 | ✅ `./dev.sh build`（提交 117b81a）。真机切机位手感待复核。 | finding_laser_roam_percamera_cropbox |
| M10.2 | ✅ **服务端按单元车位框**：migration `0020` 主键 `(bay_key,unit)`+`unit DEFAULT 'a'`(历史单框迁为 a)；repo/store/handler `GET/PUT/crop-preview ?unit=a\|b`；preview a→unitA/b→unitB 云；runner 双框各单元去背景→unitB 经 `BToA` 并入世界系→隔离并集测量(`crop_box_dual`，纯 Go)；`transformPoints` helper。 | ✅ `go test ./internal/laser/...`(`TestCropBoxPerUnit`/`TestCropPreviewUnitB` + 既有不退化)；migration 0020 up/down 对真库事务验证(PK→(bay_key,unit))。 | finding_laser_roam_percamera_cropbox |
| M10.3 | ✅ **客户端按单元贯通**：`LaserScanApi`/`LaserScanRepository`/VM `getCropBox`/`saveCropBox`/`cropPreview` 加 unit；VM `boxA`/`boxB`+`refreshCropBoxes`；融合编辑器走 unit=a。 | ✅ `./dev.sh build`。 | — |
| M10.4 | ✅ **第一视角漫游标注**：`PointCloudSurfaceView` roam 模式(walker/`frameTimeNanos` dt 积分/`applyCamera` 第一视角/路径 LINES 实体/input setters，复用 Filament 不重建)；`RoamAnnotationScreen`(全屏 look-pad 转头 + 左浮动虚拟摇杆走动 + HUD 标注/完成/重走)；`fitRoamBox` 凸包+最小面积外接矩形→`worldBox`→转交顶视编辑器微调(含翻转)。 | ✅ `./dev.sh build` + harness `laser_roam_cropbox`(`RoamBoxFitTest` 4 例：轴对齐/旋转尺寸恢复+紧包合成车+远点排除+退化回 null)。真机走动/画框/双指分流待复核。 | finding_laser_roam_percamera_cropbox |
| M10.5 | ✅ **入口闭环**：点云窗口左下「漫游标注/重标 ◈」按当前镜头进漫游(融合/A→a 框 up=地面法向、B→b 框 up=+Z)；保存到对应单元；`refreshCropBoxes` 驱动「已标→重标」；保留设备 sheet 顶视编辑器(a 框精修，亦漫游完成微调落点)。 | ✅ `./dev.sh build`。真机按镜头标注→保存→重扫按框测量端到端待复核。 | — |
| M10.6 | ✅ **harness + 文档 + 记忆**：`tests/harness/laser_roam_cropbox`(run.sh+analyze.py 跑 RoamBoxFitTest→正常/异常)；agent-memory finding；本节。 | ✅ `./dev.sh harness laser_roam_cropbox` → 正常(4/4)。 | finding_laser_roam_percamera_cropbox |

**剩（真机复核，开发期无连接设备，已以编译+单测+host harness 兜底）**：① 第一视角走动手感/速度(1500mm·s⁻¹)、look-pad+摇杆双指同时分流是否干净；② 走一圈拟合框质量(真实稀疏云)；③ 镜头 B 在自身设备系标注 + b 框测量端到端；④ 双框 `crop_box_dual` 在真车上的隔离效果。
