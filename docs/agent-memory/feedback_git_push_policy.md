---
name: Git Push 策略
description: 本地可 commit; 只在用户明确要求时 push 远端
type: feedback
---

# Git Push 策略

- 后续 Codex / Agent 完成代码或文档改动时，可以按任务需要本地 `git commit`。
- 不要自动 `git push` 到远端。
- 只有用户明确要求"push / 推上去 / 推远端"等时，才执行 `git push`。
