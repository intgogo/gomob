# sam_service 依赖。
# torch / torchvision 须用 CUDA 轮子单独装(PyPI 默认可能是 CPU 版):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# 故不在本文件锁 torch,避免拉成 CPU 版;其余依赖如下。
segment-anything-hq>=0.3,<0.4
timm>=1.0,<2       # segment-anything-hq 加载时 import(TinyViT);须 1.x(0.9 走已废弃 import 路径),非自动拉取
pillow>=9.3
numpy>=1.24,<3
fastapi>=0.110
python-multipart>=0.0.9
uvicorn>=0.27
