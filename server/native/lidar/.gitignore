# 构建产物（由 scripts/laser-cgo-setup.sh 生成；不入库）
build/
*.a
*.o
*.so
compile_commands.json
debug_output/
__pycache__/

# 大体量样本/录制/输出（保留在外部 /root/lilw/lidar，不迁入 git）
out_live/
sample/
re/
*.pcd
*.bin
# calib/ 下的小标定 yaml/json 是必需资产，不忽略
!calib/
!calib/**
